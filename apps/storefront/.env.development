# Non-secret DEV defaults, loaded by Vite in dev mode only (never prod builds).
# Points the storefront at the local API server; VITE_PREVIEW_NO_AUTH=true makes
# the shop render against the typed mock catalog so it works with no backend.
VITE_SERVER_URL=http://localhost:3000
VITE_PREVIEW_NO_AUTH=true
