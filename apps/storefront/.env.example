VITE_SERVER_URL=http://localhost:3000
# Set to "true" to render the storefront against the typed mock catalog (no
# backend). Leave unset/false in production to use the live commerce API.
VITE_PREVIEW_NO_AUTH=false
