# RetailOS Phase Roadmap

Phase sequencing per charter §31. Status tracked here; update as phases progress. The §45 reassessment loop runs at the **end of every phase** — review architecture, `lessons-learned.md`, tech debt, competitor changes (§41), performance (§44), security (§29), usability — and record improvements as ADRs.

| Phase | Scope (charter §31) | Status |
|---|---|---|
| **0 — Architecture & Foundation** | Charter review, gap analysis, domain model, ERD, governance docs, env strategy, registry/MCP + UI sourcing, design tokens, CI/test infra, Vertical Slice #1 design | **✅ Complete (locked-in)** |
| 1 — Identity, Tenant, RBAC, Audit | Better Auth plugins, tenant context, RBAC/entitlements, audit, RLS foundation | **✅ Done** (VS#1 shipped the spine — PR #1 merged to master; fail-closed RLS, 3-role model, `withTenant`, audit/outbox/idempotency) |
| 2 — Products & Inventory Ledger | products/variants/SKU/barcode, stock ledger, multi-UoM, serial/batch/expiry | **✅ FULLY COMPLETE / 🔒 FROZEN** — schema (PR #4, `d39428d`) + behavior pass (PR #9, `72b2100`: item 1 single resolver, item 3 M1 event-contract normalization, reserved nullable fields) **merged to master; CI 4/4 green**. Narrative archived in `phase-2-complete.md`. "Frozen" = any further Phase-2 work is a **change request**. **PARKED → later phases:** item 2 `cost_reconciliation` emit + POS↔costing wiring (Phase 4, #8); #6 precision/`mulDivRound` (Phase 5); #7 set-once DB-trigger backstop (Phase 3); #5 composite-FK `(tenant_id,id)` (Phase 3); `valuation_updated` totalValue/qtyOnHand enrichment (Phase 5). |
| 3 — Locations, Warehouses, Bonds | companies/locations/warehouses/bonded, transfers, bond release, bins | **✅ MERGED to master (commits 0–7, `67e6120`); CI green, db 70/70 + api 21/21 zero skips.** Graduated via PRs #18 (turbo CI fix) → #17 (commit 3) → #19 (commits 4–7). #5 composite-FK + #7 set-once trigger paid; unified self-referential `location` tree + flags + capacity seam; two-step intra-company value-conserving transfers (INV-1/2); bonded receiving + INV-3 separation (AVCO-only); bond release = approved transfer + value-only duty add (INV-4/5, RBAC-immediate); RBAC per-role split + `seedPhase3` + `phase-3-api-contracts.md`; §45 reassessment + ADR 0009. Codex-reviewed per commit. **🔒 owner decisions LOCKED:** TD-P3-1 = reject+reconcile; TD-P3-2 = defer to Phase 5. Docs: `phase-3-implementation-plan.md`, `phase-3-gap-analysis.md`, `module-specs/locations-warehouses-bonds.md`, `event-map-phase3.md`, `phase-3-api-contracts.md`, `phase-3-reassessment.md`, ADR 0009. |
| 4 — POS & Offline Queue | sale mutation, payments, receipts, shifts, offline queue, idempotency, number blocks, Tauri POS | **🚧 IN BUILD (re-sequenced by business value).** Planning complete + 4-gate-hardened `event-map-phase4.md` (POST-1/POST-2, validated vs `posting-model.md`). **Commit 1 (foundation: `mulDivRound`, fiscal_document seam, composite-FK targets) MERGED.** **Commit 2 — Minimum Sellable POS (search · cart · sale · tender · `applyValuation`/#8 · receipt · invoice) MERGED (PR #25, `a02e60b`).** **Commit 3 — Returns / Refunds / Voids MERGED (PR #26, `1aff83b`); CI 4/4 green; Codex confirm-gate CLEAN (3 HIGH folded → 2 invariant bugs fixed + 1 deferred); frozen costing byte-identical; db 83 + api 29.** `pos.exchange` **DEFERRED → "Exchange Settlement Engine"** backlog (needs the stored-value seam; contract + `exchange_group_id` seam reserved). **Offline sync ingestion foundation ported** (`offline_terminal`/`offline_sync_batch`/`offline_sync_mutation`, `pos.ingestOfflineBatch`) onto `feat/production-readiness-completion` (2026-07-02, migration 0026) — a foundation port, not a completed Commit 4; the offline conflict-policy/reconnection-avalanche work from §14 is still unbuilt. **NEXT → Commit 4 chosen by FIRST-CUSTOMER need, not blind phase order** (owner directive 2026-06-24) — default candidate is shift/cash + blind close, but a pilot's needs re-sequence it (payments-maturity / commission / numbering-lease / fiscal seam). Docs: plan, `event-map-phase4.md`, **`posting-model.md`**, `phase-4-commit3-dependency-map.md`, `event-versioning.md`, `competitive/pos.md`, `cross-phase-dependencies.md`. |
| 5 — Accounting Foundation | COA, journals, AR/AP, tax, cash clearing, money rules, gift-card/store-credit liabilities | **📋 Plan HARDENED** (Codex GL review folded (3 CRIT + 4 HIGH + 3 MED: consumer-side idempotency, event ordering/parking, structural balance/period-close); 4 open decisions surfaced) **+ foundational schema/services PORTED** onto `feat/production-readiness-completion` (2026-07-02, migration 0027): `ledger_account`/`posting_period`/`journal`/`journal_line` + `createLedgerAccount`/`createPostingPeriod`/`createDraftJournal`/`postJournal` (period-close + balanced-by-currency/scale posting trigger). This is the ledger primitive the Phase-4/6 ports post through — the hardened plan's open decisions (consumer-side idempotency for event-fed postings, ordering/parking, full period-close workflow) are still unresolved; treat as foundation, not the completed Phase-5 build. Docs: `phase-5-implementation-plan.md`, `competitive/accounting.md`. |
| 6 — Procurement | suppliers, POs, GRNs, vendor bills, landed costs | **📋 Plan HARDENED** (Codex review folded (6 HIGH + 6 MED: shared-primitive/separate-wrappers, FIFO landed-cost layer policy, AP event seam, RLS/H1); 5 open decisions) **+ substantially PORTED** onto `feat/production-readiness-completion` (2026-07-02, migrations 0027–0029): suppliers, POs, reorder-suggestion→PO conversion, GRNs (routes through `appendStockMovement`+`applyValuation`), supplier bills, landed cost pools/allocation (AVCO-only, FIFO rejected pre-write), import batches, AP posting (`postSupplierBillToAccountsPayable`) + vendor payments (`createVendorPayment`, advisory-locked, overpayment-rejecting). All H1 tuple guards applied per port (e.g. `assertReceivingLocation`, SKU-belongs-to-product). **UI built and deployed to prod (2026-07-03, master `41bc654`/`a5d669d`):** read endpoints (`supplierList`, `purchaseOrderList`/`Detail`, `goodsReceiptList`/`Detail`, `supplierBillList`, all `procurement.manage`-gated) exposed via a new `procurement.ts` router module (extracted from `vs1.ts` for file-size hygiene; also fixed a TS7056 compiler-limit bug caused by vestigial `declaration`/`composite` tsconfig flags), plus a real `/procurement` page (Suppliers tab, Purchase Orders tab with create/detail/receive/bill flows) replacing the "Planned" placeholder — the live app previously showed every procurement capability as unbuilt despite the backend existing. AP posting / vendor payments / landed-cost UI still NOT built (needs ledger-account/posting-period pickers the accounting module doesn't expose read endpoints for yet — scoped out deliberately). Docs: `phase-6-implementation-plan.md`, `competitive/procurement.md`. |
| 7 — CRM | customers, leads, opportunities, loyalty, credit limits | Not started |
| 8 — Ecommerce (Shopix) | storefront, cart, checkout, online orders, shared-inventory sync | **🚧 IN BUILD (backend steps 1–7 of 8 complete)**, sequenced against `shopix-design-and-threat-model.md` §13's 8-step build sequence, all 7 owner decisions locked (memory `shopix-v1-locked-decisions`). **Steps 1–4 (hostname→tenant gateway, public catalog/PDP, real tax engine, PII vault/guest customer/cart) MERGED to master.** **Step 5/6/7 (checkout+reservation, order→GL events via the real sale/tender document, mock payment provider) BUILT on `master`, gated, not yet committed** (2026-07-03) — `commerce-checkout.ts`: `createCheckoutOrder` (coarse non-binding placement) + `confirmCheckout` (canonical lexicographic per-cell advisory-lock ordering, atomic availability gate with a generic `COMMERCE_UNAVAILABLE` error, frozen `appendStockMovement`+`applyValuation` invoked unconditionally, real `sale`/`sale_line`/`tender` write, `sale.created`/`payment.received` emitted with POS's exact payload shape channel-marked "online" — zero new GL consumer code needed later). New `order`/`order_line` schema (`sale_id`/`number` NULL until paid). Idempotent via server-minted `checkoutIntentId` (`runIdempotent`), never client-supplied. **Proven by a genuine concurrency test** (two real separate-transaction confirms racing for the last unit — exactly one wins, stock never goes negative) — the design doc's own load-bearing claim. db 141/api 83 zero skips, frozen costing untouched. **v1 simplifications documented, not silently narrowed:** one sellable location + one active SKU per product required; no real rate-limit budget (Redis token bucket) yet; confirm reuses the create-time price snapshot (synchronous mock provider). **REMAINING: Step 8 (storefront UI) only** — explicitly deferred, backend-only per the Assembly Law; no UI has been built for any Shopix step. Docs: `shopix-design-and-threat-model.md`, `commerce-experience-capability-map.md`, `commerce-ui-plan.md`. |
| 9 — Hardware Bridge | abstraction, print protocol, Tauri bridge, daemon, device registration | Not started |
| 10 — Edge Hub | Dockerized hub, LAN sync, local coordination, cloud sync, reconnection testing | Not started |
| 11 — SaaS, Licensing, White-Label | billing, feature flags, license model, custom domains/SMTP/branding, entitlements | Not started |
| 12 — Analytics, Reporting, Insights | dashboards, reports, correlated insights, exports, read models | Not started |
| 13 — Enterprise Hardening | observability, security, DR, perf, multi-region, compliance roadmap | Not started |

## Phase 0 — what's done vs remaining

**Done (committed):**
- UI component inventory (`ui-inventory/`, 10 files), live-verified across shadcn core/studio Pro/Magic UI free+Pro/ReUI; registry config corrected (root + `packages/ui`); `lessons-learned.md` (12+ entries); `.claude/CLAUDE.md`.
- This pass: charter committed; root `CLAUDE.md`; ADRs 0001–0005; module-spec template; glossary; this roadmap; competitive program; architecture-review doc set; Vertical Slice #1 design; CI/test/docker foundation config; Docker image optimization (2.38GB→168MB) + dependency alignment.

**Phase-0 lock-in (§46): COMPLETE** — `check-types`/`check`/`test` green, CI green on master, Docker image builds + boots. A 2026-06-21 audit-only red-team pass (`phase-0-audit.md`) found **zero CRITICAL / zero HIGH**; stale-metadata MEDIUMs resolved; engineering items queued. Phase 1 begins.

> The first **feature** work is Vertical Slice #1 (§32), implemented only after this architecture review is approved (ADR 0002).
