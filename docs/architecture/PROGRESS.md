# RetailOS — PROGRESS (live cross-agent state)

> **Read this first.** This is the single live operational tracker for the current effort, shared across
> agents and across context compaction. It holds the **task board**, a **changelog**, and **verified facts**
> so no agent re-derives or re-litigates settled work. Update it as part of every commit.
>
> Distinct from: `phase-roadmap.md` (strategic Phase 0–13 status) and `phase-0-checklist.md` (§46 gate
> scoreboard). This file is the day-to-day operational log.

<!-- ═══════════════════════════════════════════════════════════════════════════
     AUTONOMOUS OVERNIGHT RUN — read this block FIRST in the morning
     ═══════════════════════════════════════════════════════════════════════════ -->

## 🌙 RUN STATUS (top-of-file; cross-agent state)

### Commerce back-office + product media (in progress, 2026-06-28)
- **Branch:** `feat/commerce-backoffice-products` off `master = a98390b` after PR #45 was merged. Owner asked Codex to push work forward on a separate branch for other agents to vet.
- **Scope built so far:** backend product-media seam + CommerceO-style Dashboard/Products polish + POS post-sale console. Added tenant-owned `product_image` with composite FK `(tenant_id, product_id) → product(tenant_id, id)`, single-primary partial unique index, fail-closed RLS, Drizzle snapshot/journal metadata, `product.imageCreate`, and DTO-safe `product.detail`. `product.catalog` now returns only display-safe primary image fields (`primaryImageUrl`, `primaryImageAltText`) and still leaks no costing/policy/object-key internals. Frontend Products now renders a CommerceO-style product table with thumbnails and links to `/products/$productId`; Dashboard has a catalog spotlight panel; product detail has a Studio-inspired media manager (probed `product-list-01`, `form-layout-02`, `file-upload-01`, `empty-state-01`) backed by the real media mutation. Added `/sales` as an owned Studio/dashboard-shell-style POS sales console using existing `pos.saleSearch`, `pos.saleDetail`, `pos.receipt`, and `pos.void`: recent-sale lookup, receipt lines, refund-state summary, backend `availableActions`, receipt reprint, and a guarded whole-sale void dialog with a stable idempotency key per attempt. App shell/sidebar unchanged per locked decision except adding the Sales nav item under Sell.
- **Verification:** `bun run check`, `bun run check-types`, `bun run check:mojibake`, default `bun run test`, and `bun -F web build` green. Fresh disposable PG18 on port 56432 (`roles.sql` → migrate 0000→0022 as `retailos_migrator` → test as `retailos_app`) green for the backend/media slice: **db 93/93 + api 53/53, zero skips**. Note: direct `shadcn add @ss-blocks/... -c packages/ui` was blocked in this shell because `EMAIL`/`LICENSE_KEY` were not exported; MCP registry probing succeeded, so the UI was ported/adapted as owned code rather than raw-installed. For the `/sales` UI slice, Studio registry search was probed (`dashboard-shell-01`/sales-table-style matches) and the surface was adapted as owned code from installed shadcn primitives because no exact post-sale block exists.
- **Found/fixed:** hand-written SQL migration alone was not applied by Drizzle because `_journal.json`/snapshot metadata were missing; fixed by regenerating `0022` metadata from schema and preserving the hand-required RLS block. Lesson appended.

### 🚀 FIRST DEPLOYMENT — LIVE (2026-06-28)
**RetailOS is live at https://retailos.karetechsolutions.com (api: retailos-api.karetechsolutions.com).** Branch `feat/deploy-server-image-and-auth` (PR #44) — deployed FROM the branch; merge to make `master` match prod.
- **Infra:** `retailos-web:3001` + `retailos-server:3000` containers on the `pangolin` network (Traefik/SSL via Pangolin resources 68/69). `retailos` DB migrated on `postgres-central` (48 tables, RLS forced; runtime connects as `retailos_app`, `bypassrls=false`). All secrets in **Infisical** `/credentials/retailos` (DATABASE_URL/auth/cross-subdomain); deploy via `infisical run` (never on disk/git). NTFY-notified.
- **Auth verified live:** sign-in 200, `activeOrganizationId` auto-set (the #41 hook), cross-subdomain cookie `.karetechsolutions.com` (web ↔ api). `pos.itemSearch` returns seeded products authed.
- **Deploy bugs found+fixed (in PR #44):** web image never baked `VITE_SERVER_URL` (Vite reads `.env`, gitignored ⇒ write it from the build arg); compose runtime-secret wiring (`${VAR}` via `infisical run`, no infisical-entrypoint in the images); server image `node_modules` (Bun isolated-install symlink tree + root `.bun` store); **`/` showed the Better-T-Stack scaffold + sign-in went to scaffold `/dashboard` ⇒ `/`→`/pos` redirect + sign-in/up→`/pos`.**
- **Demo seed (one-off script, removed):** `admin@retailos.demo` / `retailos-demo-2026` → org `org_retailos_demo` + Main Store + 3 products + 300 units stock.
- **Follow-ups:** merge PR #44; formalize the demo seed into a committed `seedDemo` if repeatable demo tenants are wanted; the scaffold `_auth/dashboard` + `healthCheck`/`privateData` endpoints are now unreachable (harmless; can be removed in a cleanup).

### First-deployment prep — server image + cross-subdomain auth (in PR, 2026-06-28)
- **Why:** RetailOS is on a VPS (no `localhost` for the owner) and the **backend was never containerized** (prod compose built web only; `retailos` DB on `postgres-central` is empty). So "view a working RetailOS at a URL" = its **first real deployment**. This PR is **steps 1–2 only** (reversible, repo-only, zero production touch); steps 3–6 (DB migrate/seed, container run, Pangolin DNS) are **owner-supervised**, gated on creds.
- **Branch:** `feat/deploy-server-image-and-auth` off `master = d7b8653`.
- **Built:** (1) **`apps/server/Dockerfile`** — multi-stage Bun image (turbo prune → tsdown bundle → distroless). `tsdown` inlines `@RetailOS/*` but externalizes npm deps, so the runtime ships `dist` + the Bun isolated-install `node_modules` (root `.bun` store **and** the workspace symlink tree at the same relative depth — the boot-fail-then-fix). **Verified: builds → boots → serves `/`=200 → `/api/auth/ok`={"ok":true}.** Added `server` service to `docker-compose.prod.yml` + `container_name`s (`retailos-web`/`retailos-server` — fixes the generic-`app`-on-`pangolin` DNS-collision footgun).
- **Auth (verified vs Better Auth docs):** cookies **env-gated** — prod (HTTPS) keeps `secure`+`none` (never weakened), local http dev relaxes to `lax`+insecure; **`crossSubDomainCookies`** (domain from new optional `AUTH_COOKIE_DOMAIN` env, never hardcoded) shares the session across `retailos` ↔ `retailos-api`; **`databaseHooks.session.create.before`** sets `activeOrganizationId` to the user's single `member` org (closes #41 for single-org; defensive — a lookup failure can never block login), via new `db.resolveSingleMembershipOrg`; prod-only `ipAddress.ipAddressHeaders:["x-forwarded-for"]` (behind Traefik). Gates: check-types 7/7, lint 0, mojibake 0.
- **🔒 Steps 3–6 need (owner-supervised):** `retailos_migrator` password (Infisical) to migrate the `retailos` DB; a seed (admin user + org + location + products); Infisical machine token for the compose deploy; Vault `secret/pangolin/api_key` for the **Integration API** (OrgId `kqretech-solutions`, SiteId `1`, DomainId `domain1`) → 2 resources `retailos`+`retailos-api` (API path only — never touch Pangolin SQLite / never restart it). **Status:** PR open → STOP (no merge, no production touch).

### Overnight autonomous run — outcome (2026-06-28)
Independent-branch fan, Codex-reviewed each, owner-gated merges. Result: **#40→#39 merged** (unblocked Task 1) → **Task 1 = PR #43 (`pos.saleDetail` + `availableActions`) MERGED** (Codex caught a real `canRefund`↔shift HIGH pre-merge; folded via a shared `findOpenShift`) → **Task 2 = PR #42** (`module-entitlements-design.md`, owner-gated draft) → **Task 3 = PR #41** (`auth-login-recon.md` — login is greenfield; needs an org-selection step). The merge gate held throughout.

### Frontend-readiness slice 5: `pos.saleDetail` + `availableActions` (in PR, 2026-06-28)
- **Branch:** `phase-4-pos-sale-detail` off `master = f9fc975` (post-#40 governance + #39 `saleSearch` merged). The per-sale read the post-sale ACTION view needs — built backend-first/separate (the `saleSearch`/`itemSearch`/`locationList` pattern). The first build **governed by principle #20 (Action Availability)** — landed on master via #40 ahead of this.
- **Scope (one PR, one read + one service fn):** **`pos.saleDetail`** — cashier-safe (`pos.create_sale`), tenant-scoped (RLS), keyed by `saleId`. Returns the **receipt display model** (reused `services.buildSaleReceipt` — named lines/totals/status, receipt-safe, **no COGS**) + per-line **`refundState`** (`none|partial|full`, with qty/refundedQty/refundableQty) + **`availableActions {canReprint, canRefund, canVoid}`** computed **server-side** from the caller's REAL grants AND sale state. New service `services.buildSaleDetail` (in `receipt.ts`) gathers the data + `flags`; the router's authorization layer turns flags+role into the booleans.
- **#20 corollary VERIFIED (not just rendered):** `pos.refund`/`pos.void` already `assertPermission` server-side (`pos.refund` / `pos.void_sale`) — confirmed in-code; no fix needed. `availableActions` **mirrors the EXACT `runVoid`/`runRefund` guards** (canVoid = grant ∧ saleType="sale" ∧ completed ∧ no-prior-returns; canRefund = grant ∧ completed ∧ saleType="sale" ∧ refundable-remaining) so a rendered action can't be rejected at submit (no availability↔enforcement drift — the quote/createSale lesson). UI visibility is never the guard.
- **Test (real Postgres, fresh disposable PG18, as `retailos_app`):** +1 cohesive router test in `vs1.integration.test.ts` — fresh sale (admin both actions / cashier reprint-only, same state → only role differs), partial refund (status `partial`, canRefund yes / canVoid no), full refund (status `full`, canRefund no), voided (both no), cross-tenant `saleId` → NOT_FOUND, no-membership rejected, DTO no-leak (no `cogs`/`margin` in the response), and the **#20 corollary** (cashier rejected by BOTH `pos.refund` and `pos.void` endpoints — `Missing permission:` asserted).
- **Gates:** check-types 7/7, lint 0, mojibake 0; disposable PG18 (roles→migrate 0000→22→test as `retailos_app`): **db 93/93 + api 52/52, zero skips**. Coordinator-run `codex:codex-rescue` → fold → STOP.
- **Status:** built overnight (Task 1 of the independent-branch fan, unblocked once owner merged #40→#39). PR open → **STOP** (no merge; awaiting owner). Does NOT build the cashier post-sale FRONTEND workflow (that consumes this read once merged — next supervised session).

### Frontend-readiness slice 4: `pos.saleSearch` (in PR, 2026-06-27)
- **Branch:** `phase-4-pos-sales-find` off `master = 4b843ba` (post-#38, Frontend MSP Commit 1 merged). The read contract that unblocks the **POS-loop frontend capability** (sales lookup → receipt reprint / returns / void) — built FIRST and SEPARATE so the frontend PR stays pure (the `itemSearch` / `locationList` frontend-readiness pattern). Backend read only.
- **Capability decision (owner):** the next frontend capability is **deepen the POS loop** (returns/void + receipt reprint), NOT inventory receiving — receiving's real form (receive-against-PO) needs unbuilt procurement (Phase 6) and pulls toward the fenced IoT device-write class; the POS loop runs on already-merged contracts (`pos.refund`/`pos.void`/`pos.receipt`), same user. The ONLY gap was "find a recent sale to locate its saleId" → this slice.
- **`pos.saleSearch`** — cashier-safe (`pos.create_sale`, NOT admin/reporting), tenant-scoped (RLS), returns ONLY sale-level find fields `{id, number, saleType, status, totalMinor, currency, scale, createdAt, locationId}` — no line/COGS/margin/subtotal/rep/customer internals (sale-only select, can't leak `sale_line`). **BOUNDED:** a **30-day recency floor** (load-bearing) + most-recent-first + hard `limit` (max 50) + optional number match + optional location scope. An operational find, NOT a history export.
- **Codex (coordinator-run — fork self-review is not the gate):** 0 CRIT / **1 HIGH** / 0 MED / 2 LOW, **all folded.** HIGH — without a recency window the `limit` only caps a PAGE, so scripted number searches could walk the tenant's entire history 50 rows at a time → added the 30-day `createdAt` floor (a longer per-tenant refund window is a future settings-resolver concern). LOW1 — number-search test was vacuous → now asserts every row matches `q` + a non-matching `q` returns empty. LOW2 — added a `limit:51`-rejected assertion. Tenant-isolation, gating, DTO-leak, correctness, lint all CLEAN.
- **Gates (disposable PG18, roles→migrate 0000→22→test as `retailos_app`):** check-types 0, ultracite 0, mojibake 0, **48/48** integration tests (+1 `saleSearch`: cashier-safe, no-membership-rejected, no-leak, bounded, recency-floor-enforced via backdate, number-filter, limit-clamp), zero skips. Diff +~130 lines.
- **Status:** opening PR → **STOP** (no merge; owner review). The POS-loop frontend capability does NOT start until this read merges. Pending (separate small change): institutionalize the **one-writer-per-tree** rule in `engineering-principles.md`.

### Frontend MSP — Commit 1: app shell + POS checkout slice (✅ MERGED PR #38 → `master = 4b843ba`, 2026-06-27)
- **Branch:** `frontend-msp` off `master = f993a5e` (post-#37). First RetailOS frontend slice. **Single-till / online-only** (number leasing not yet bound to createSale — honoring the locked decision below). UI ASSEMBLY, not invention.
- **Scope (all 12 items, one PR, render-only against the cashier DTOs):** app shell (sidebar/header/nav) · POS checkout route · location selector (`pos.locationList`, auto-select single-store / picker multi-store) · product search + barcode-scan-add (`pos.itemSearch`) · cart (client `useReducer`, no Zustand dep) · quote panel (`pos.quote`) · tender dialog (re-quotes with the tender so **settlement/change/settleable come from the backend**) → `pos.createSale` · receipt preview (`pos.receipt`, null seams handled) · loading/error/empty + online/offline states · light/dark (next-themes) · tablet-first responsive.
- **DTO discipline:** every POS type is **derived from the backend router client** (`apps/web/src/components/pos/types.ts`) → the UI cannot drift from the contract (a removed/changed DTO field fails type-check). Frontend computes **no money** — every total/tax/line-total/change/balance is a DTO field; the only arithmetic is presentation (`formatMoney`/`minorToAmountString` display conversion) and tender input parsing (`toMinor`).
- **Sourcing (sourced-vs-hand-rolled):** app shell ← owned shadcn `sidebar` block (the AdminCN-style primitive); all primitives ← shadcn/ui owned in `packages/ui` (added + used: sidebar, sheet, dialog, tabs, select, badge, separator, tooltip, alert — unused installs removed to keep the surface minimal; one `biome-ignore` for the canonical shadcn sidebar state-cookie). Hand-rolled ONLY where no sourced block exists (per `gaps-and-custom.md`): the online/offline indicator (Badge+Tooltip) and the empty-state pattern (no shadcn Empty primitive here) — both built on sourced primitives.
- **Boundary audit (security):** cashier flow calls **ONLY** `orpc.pos.{locationList,itemSearch,quote,createSale,receipt}` — no admin/catalog/inventory/company/product route reachable, even transitively. No fake/mock/demo data. No `next/*` framework imports (next-themes is a library). Verified by grep + the coordinator's independent boundary audit.
- **Also fixed a gap (+ a CI fix on top):** `apps/web` had **no `check-types` script** → it was absent from the CI type gate. The fork added `"check-types": "tsc --noEmit"`, but that was green only LOCALLY (the gitignored TanStack `routeTree.gen.ts` already existed) and went **RED in CI** (fresh checkout has no route tree → every route `tsc`-fails). Coordinator fix: `check-types` = **`tsr generate && tsc --noEmit`** (+ `@tanstack/router-cli` devDep) so CI regenerates the route tree before type-checking. Verified by deleting `routeTree.gen.ts` and re-running. Lesson: a gate is only real in the environment it runs in — local-green with a stale generated artifact ≠ CI-green.
- **Adversarial review (coordinator-run `codex:codex-rescue`, the fork couldn't spawn it):** **0 CRITICAL / 1 HIGH / 1 MEDIUM.** **HIGH FOLDED** — `tender-dialog` minted a fresh `crypto.randomUUID()` idempotencyKey on every `pay()`, so a double-click / post-timeout retry created TWO distinct sales the backend couldn't collapse; fix = ONE stable key per checkout attempt (minted on dialog-open, reused for retries) + a `pay()` in-flight guard. **MEDIUM DEFERRED (documented, per "fold CRIT/HIGH only"):** `product-search` hand-rolls an autocomplete (`Input` + custom `<ul>`) instead of the shadcn `Command` primitive (`cmdk` is already a dep) — a sourcing-law nit + lost listbox a11y; **follow-up: swap to a Command/Combobox in a later commit.** Boundary audit, no-money-math, null-seams, no-Next all CLEAN.
- **Gates (after folds):** `bun run check-types` **7/7** (web regenerates its route tree), `ultracite check` **0**, `check:mojibake` **0**, `bun -F web build` **PASS**. No tests added (skeleton UI; backend logic already covered). Visual/E2E via CI preview env.
- **Status:** PR **#38** open → coordinator folded the HIGH + the CI route-tree fix → **STOP** (no merge; awaiting owner review). Does NOT start additional screens or backend Commit 7. Open follow-up: product-search → Command primitive (Codex MED).

### 🔒 Frontend-MSP prerequisites — decisions LOCKED (2026-06-27, post-#36)
Recorded **before any frontend work** so none of these disappear by omission. `master = ba82f46` (PR #36 merged — `pos.locationList` closed the last hard read-contract gap; the frontend-readiness reads `pos.itemSearch` / `pos.quote` / `pos.locationList` + `pos.receipt` are all merged).

- **🔒 `pos.bootstrap()` — DO NOT BUILD (now).** A single aggregate POS-startup endpoint is *premature aggregation* (a god endpoint). The cashier flow already has the discrete reads it needs — `pos.locationList`, `pos.itemSearch`, `pos.quote`, `pos.receipt` (+ Better Auth session for the active tenant). **If** startup latency later becomes a *measured* problem, implement it as a **thin composer** that parallel-fans-out those existing reads (no new source of truth), **never** a god endpoint that re-derives state.
- **🔒 `TENDER_METHODS` — shared CONSTANT, not a runtime endpoint.** `schema.TENDER_METHODS` is a *static* enum. When the frontend tender panel needs the valid methods, expose them as a shared constant/export (from the shared schema/constants package), **not** a runtime `pos.tenderMethods` endpoint. Static enums do not get network round-trips.
- **🔒 Number leasing (Commit 6) — MERGED but NOT bound to the sale path ⇒ Frontend MSP is SINGLE-TILL / ONLINE-ONLY.** The distributed lease allocator is merged (PR #34): `number_lease`/`number_lease_usage` schema + `pos.numberLease{Allocate,Current,Consume,ReportSkipped,Reclaim}` routes, with global disjointness under concurrency/replay/reclaim. **BUT** `pos.createSale` still mints document numbers via the single-node `allocateSaleNumber` (advisory-locked `count()+1` → `SALE-n`/`INV-n`), **not** the lease — the lease↔`createSale` binding (the #8 seam) is deferred to the offline-queue commit (per Commit-6 scope). **Conscious temporary constraint:** Frontend MSP ships **single-till / online-only**; **no multi-terminal or offline production rollout until number leasing is bound to `createSale`** with global disjointness under concurrency/replay/reclaim. Do not let this disappear by omission.
- **🔒 IoT / device write-sources — NOT in Frontend MSP; define the INVARIANT before any device-integration commit.** Devices (biometric clocks e.g. ZKTeco, scales, scanners via a local bridge, EFTPOS terminals, utility meters on WISP sites) are a NEW class: **write sources that originate OUTSIDE the authenticated browser session and often outside reliable connectivity.** They re-open the exact three questions number leasing just closed: (1) **authenticated device identity** — a device has no Better Auth session, it carries a device credential, so a new auth path must map device → tenant + permission set; (2) **tenant resolution WITHOUT bypassing RLS** — RLS guards the *session's* tenant, but a device write may arrive with NO session, so a wrong-tenant device write is a cross-tenant leak through a path RLS was never asked to guard; (3) **idempotency under retry** — a flaky link retries, so a duplicate punch / reading / tender is the IoT analogue of the double-issued document number. **Reference shape:** the Heimdallone ZKTeco attendance bridge (device → tenant-scoped write) — but that was SAFE only because it was display-only on already-true-UTC instants; the next device may drive payroll/billing, where a duplicate or wrong-tenant write has financial consequences. **Rule:** each device integration is its OWN slice with its OWN auth-and-idempotency contract and its OWN adversarial review, sequenced AFTER the frontend can render the data those devices feed — never braided into screen-assembly (the frontend renders; devices write; different threat models). Define device-identity / tenant-resolution / replay-idempotency as a contract FIRST (the event-map / numbering-disjointness pattern), then build. Same defect class to avoid as the multi-till trap: an integration that "works" in a single-device / good-connectivity / one-tenant demo, hiding the auth/tenant/replay holes until a second site comes online.

### Phase 4 — Frontend-Readiness slice 3: POS location picker (✅ MERGED PR #36, 2026-06-27)
- **Branch:** `phase-4-pos-location-list` off clean `master` (`d5446c6`, post-#35). The THIRD frontend-unblocking backend read — same class as `pos.itemSearch` (createSale needed `skuId` the FE couldn't get → itemSearch; now quote/createSale need `locationId` the FE couldn't get → this). Backend read only; no frontend code; architecture/UI strategy stay frozen.
- **Scope (one PR, one endpoint):** **`pos.locationList`** — cashier-safe (`pos.create_sale`, NOT any `products.*`/admin grant), tenant-scoped (RLS), returns ONLY **sellable + non-archived** locations (a non-sellable one is refused by `assertSaleLocation`/INV-P4-7, so the picker only ever offers a location `pos.quote`/`pos.createSale` will accept) with ONLY POS-picker fields `{id, displayName, companyId, type, isSellable, isBonded, isTransit}` — no shift/cash/costing/hierarchy internals, no mutation. Returns `{autoSelect, locations[]}` where **`autoSelect` = exactly one eligible location** → single-store tenant skips the dropdown (login→auto→POS); multi-store gets a picker. Same contract, business-size-adaptive UX (small↔enterprise, one codebase).
- **Tests (real Postgres):** +1 router test in `vs1.integration.test.ts` — fresh tenant (deterministic count); sellable-only (excludes a direct-inserted non-sellable bonded location); `autoSelect` true at 1 store → false at 2; DTO leaks no management/hierarchy/cash internals; **no-membership caller is rejected** (cashier gate is real).
- **Codex adversarial review (tenant-isolation / cashier-safety / DTO-leak / autoSelect / sale-location consistency / test rigor):** 0 CRIT; **1 HIGH + 1 LOW, both folded.** HIGH — an `includeArchived` flag (reflexively mirrored from admin `*.list`) let a cashier surface retired-but-sellable locations, contradicting the contract → **removed entirely; non-archived is now unconditional.** LOW — added the missing permission-denial regression. Tenant-isolation, gating, leak, autoSelect, and filter-consistency all came back CLEAN.
- **Verification:** check · check-types · mojibake green; disposable PG18 (`roles.sql`→`db:migrate` 0000→22→test as `retailos_app`): full gate **7/7 turbo tasks, api 50/50**, zero skips.
- **Status:** ✅ **MERGED — PR #36 → `master = ba82f46`**, merge-commit CI green (Quality gates · RLS real-Postgres · E2E · Docker); both branches deleted. Frontend MSP does not start until the prerequisite decisions above are honored.

### Phase 4 — Frontend-Readiness slice (POS item DTO + cart quote) (in progress, 2026-06-27)
- **Branch:** `phase-4-frontend-readiness` off clean `master` (`483ec4a`, post-#34). Narrow frontend-unblocking backend slice; NOT a broad Phase-4 reopen. No frontend code.
- **Scope (one PR, two endpoints):**
  - **`pos.itemSearch`** — cashier-safe (`pos.create_sale`, NOT `products.create`), SALE-LINE-READY catalog read returning `skuId` + price + sellable so the POS never calls the admin `catalog.skuList`. SKU-level rows (productId, skuId, displayName, productName, skuCode/skuName, matchedBarcode on scan, priceMinor/currency/scale, trackingMode, sellable). Sellable-only filter (product live, sku active+live). Exposes NO admin internals (no costing/oversell/policy fields).
  - **`pos.quote`** — read-only pre-sale cart/tender preview. Reuses the SAME `priceMspLines` + `settleTenders` `pos.createSale` uses (literally the same code path → cannot drift). Returns receipt-preview-compatible `{currency, scale, lines[], taxBreakdown[], totals{subtotal/discount/tax/total}, payments{items, summary{tendered/settled/change/balanceDue}, settleable, underpaid, settlementError}}`. Underpayment / cash-overpayment are FLAGGED (not thrown). Creates NO sale/invoice/tender/stock/number/outbox (pure SELECTs).
- **Deferred (NOT built; tracking only):** lease↔createSale binding, offline queue, expiry sweeper, fiscal provider/fiscal_document, exchange settlement, commission, gift-card/store-credit ledger, accounting/GL. Stock-availability summary on item search deferred (no cheap unified on-hand read; D5 allows oversell anyway).
- **Tests (real Postgres):** 7 new router tests in `vs1.integration.test.ts` — cashier search returns skuId + barcode match; cashier blocked from admin `skuList` + DTO leaks no internals; quote totals == createSale totals (same cart); cash overpayment/change; underpayment flagged consistently with createSale's rejection; quote mutates nothing (sale/invoice/tender/ledger/outbox counts unchanged); cross-tenant sku rejected.
- **Codex adversarial review (frontend-duplication / quote-drift / admin-leak / quote-mutation):** 1 HIGH, folded. **HIGH** — `pos.quote` skipped the `resolveSaleShift` shift/terminal enforcement `pos.createSale` runs, so a green preview could be rejected at submit for a shift reason (quote/createSale drift). **Fix:** `pos.quote` now accepts optional `shiftId`/`terminalId` and runs the SAME `resolveSaleShift` (read-only — SELECTs shift rows only, no mutation) before pricing/settlement; +1 regression test proving quote and createSale reject an invalid shiftId identically. Q1/Q3/Q4 came back clean (itemSearch returns skuId+price with no admin-internal leak; quote is SELECT-only).
- **Verification:** check · check-types · mojibake green; disposable PG18 (roles→migrate 0000→0021→test as `retailos_app`): **api 48/48, db 93/93**, zero skips.
- **Status:** review folded + re-verified → opening PR → STOP (no merge without owner approval).

### Phase 4 Commit 6 — Number-Block Leasing (in progress, 2026-06-25)
- **Branch:** `phase-4-commit-6-number-leasing` off clean `master` (`407b542`).
- **Scope:** backend-only distributed number leasing. Architecture/governance/UI strategy remain frozen; no frontend work.
- **Invariant:** global disjointness under concurrency, replay, expiry, and reclaim. Leased numbers are never reminted; unused tails are recorded as skipped usage rows for fiscal gap auditability.
- **Implementation status:** added `number_lease` + `number_lease_usage` schema/migration with fail-closed RLS, composite FKs, allocation request hashing, idempotency key uniqueness, and a trigger backstop rejecting overlapping ranges per tenant/block. Added allocator service using advisory locks + `number_block.next`, replay-safe allocation, cursor-based consumption with duplicate-retry recognition, explicit skipped-number reporting, expiry/terminal-guarded reclaim, and minimal POS API routes for allocate/current/consume/skip/reclaim.
- **Codex review:** initial independent review found 0 CRITICAL / 4 HIGH (allocation mismatch replay, consume/skip retry, block scope mismatch, reclaim ownership/expiry) — all folded with regression tests; confirmation review came back **CLEAN (0 CRIT/HIGH)**. A follow-up **Frontend Readiness Audit** returned 0 CRIT / 2 HIGH that are *forward seams, not Commit-6 defects*: (1) POS-item read-model/DTO (`product.search` returns raw product rows but `createSale` needs `skuId`) → first task of the frontend POS slice; (2) lease↔`createSale` binding (the #8 seam) → deferred to the offline-queue commit (per Commit-6 scope: no sale binding yet).
- **Post-review hardening (Claude vet pass, 2026-06-27):** fixed a LOW latent **int4 overflow at the block ceiling** — the overlap trigger `int4range(range_start, range_end + 1)` and the `number_lease_next_chk` CHECK `next_number <= range_end + 1` both overflow int4 when `range_end` = int4 max (reachable only via a legacy count-based block at the ceiling). Hardened to be overflow-proof regardless of block origin: trigger now `int8range(...::bigint, ...::bigint + 1)`; CHECK rewritten to the equivalent overflow-safe `next_number - 1 <= range_end`; service `DEFAULT_LEASE_RANGE_END` capped to `2_000_000_000`; `getCurrentNumberLease` now excludes time-expired-but-not-flipped leases (`expiresAt > now()`) so it never hands back a lease `consume` would reject. +1 regression test (`overlap backstop … int4 ceiling`) proving the backstop rejects an overlap cleanly (not via overflow) at `range_end = 2_147_483_647`. schema + migration + snapshot kept in sync.
- **Verification:** `bun run check`, `bun run check-types`, `bun run check:mojibake` green. Real Postgres (fresh disposable PG18, `roles.sql` → `db:migrate` 0000→0021 from scratch → test as `retailos_app`): **db 93/93** and api **40/40**, zero skips.

### 🧭 PHASE 3 — RESUME HERE (cold-session actionable)
- **master HEAD = `dbe77e2`** (post-PR #16). **Commits 0+1+2 MERGED, all CI-green.** **COMMIT 3 IMPLEMENTED on branch `phase-3-commit-3` (off `dbe77e2`) — STOPPED for owner review, NOT merged.** This commit touches frozen `costing.ts` (additively) → owner routes it through Codex before merge.
- **✅ COMMIT 3 (transfer VALUE conservation) — DONE on branch, all gates green:**
  - `costing.ts` **+207 / −0** (purely additive): new `applyTransferInValuation`. **AVCO** reuses the frozen receipt (`floor(V/q)`) + value-only (remainder `r`) seams → net exactly `+V`, **zero AVCO change**. **FIFO** = new ADDITIVE primitive: value-EXACT integer layer set `(q−r)@floor(V/q)`, `r@ceil(V/q)` (Codex-F1), aggregate/not-source-age-preserving.
  - **HARD GATE PASSED:** `git diff master -- costing.rls.test.ts` is **EMPTY** (byte-identical) and `costing.ts` is **+207/−0** — the existing Phase-2 costing suite passes UNEDITED (the additivity proof). No existing costing test was touched.
  - **Schema-vs-plan collision found + resolved with ZERO schema change:** the F1 2-layer set collides with `valuation_layer`'s `UNIQUE(tenant_id, source_movement_id)` (FIFO-receipt replay guard). Resolved by anchoring the two layers to the leg's **two real distinct movements** (base→`transfer_in`, remainder→paired `transfer_out`) — no constraint relaxation, replay-idempotent. (lessons-learned entry added.)
  - `transfer.ts`: `moveLine` valuates BOTH legs for SKU lines (product-level = qty only, mirrors `inventory.receive`); ship/receive/cancel return `{transfer, lineValues}`. Value flows source→per-transfer transit→dest, conserved at each step. Cancel-after-ship returns value to source (no orphan in the transit node).
  - Router (`vs1.ts`): `transfer_dispatched`/`transfer_received`/`transfer_cancelled` now carry **per-line** value + a **top-level aggregate** (single-currency). event-map-phase3 updated.
  - **Tests:** new `transfer.value.rls.test.ts` (AVCO conservation; FIFO conservation + F1 penny `V=201,q=2`→exactly 201; F2 two overlapping transfers don't blend) + INV-2 write-path gate in `vs1.integration.test.ts` (router invokes valuation on BOTH legs for AVCO+FIFO; event value fields). **db 45→48, api 16→17.**
- **🚨 DISCOVERED + FIXED — CI was SILENTLY SKIPPING every DB-gated suite.** The last master `db-rls` CI log shows `db 22 passed | 23 skipped` (costing/inventory/tenant/services RLS all skipped) + `api 13 skipped` — **turbo 2.x strict env mode** strips `RLS_TEST_DATABASE_URL` from the vitest child, so every `skipIf(!url)` suite skipped IN CI. The "real-Postgres RLS green" claim was the non-DB unit tests. **Fix:** added `passThroughEnv` to the `test` task in `turbo.json`; verified `bun run test` (turbo) now RUNS db **48/48** + api **17/17** (zero skips) vs real Postgres. (lessons-learned entry added.) **Owner: this is a repo-wide CI correctness fix bundled into commit 3 — flag if you want it split out.**
- **Gates (local, real Postgres throwaway PG18, bootstrap→migrate 0000–0014→test):** check 163/0 · check-types 6/6 · mojibake clean · **db 48/48 · api 17/17** (all DB suites RUN, none skipped) · costing suite UNEDITED.
- **✅ COMMIT-3 CODEX REVIEW DONE (2026-06-22):** the two flagged areas **HELD** — FIFO two-movement anchoring is replay-safe/leak-free (no issue path keys a layer to a transfer_out id; degenerate cases exact) and AVCO additivity/invariants hold. Codex found **2 HIGH + 1 MEDIUM, all FIXED on the branch:** (HIGH-1) transfer status race — `loadTransfer` now `SELECT … FOR UPDATE` serializes ship/receive/cancel (a pre-existing commit-2 race that commit-3 value amplified); (HIGH-2) turbo cache correctness — moved the skip-deciding vars from `passThroughEnv` to **`env`** (hash-affecting → no stale-skip replay; still passed to child); (MEDIUM) `transfer.create` now `assertSkuBelongsToProduct`. +2 regression tests (concurrency, cross-ref). Re-verified: check/check-types/mojibake green; real-Postgres **db 48/48, api 19/19**, costing suite still byte-identical. **STOPPED for owner approval — NOT merged.** Owner to decide: bundle the turbo fix or split to its own PR (agent rec: split-first).
- **AFTER commit 3 merges → COMMIT 4 (bonded receiving + separation), then 5 (bond release + duty), 6 (RBAC+seed+contracts), 7 (§45 + ADRs).** Bonded = AVCO-only (§I.4).
- **Open PRs:** **#12** (design-language docs — separate, decide independently).

- **Branches/PRs:** Phase 1 (PR #1), Phase 2 (PR #4/#9), Phase 3 commits 0+1+2 (PR #13/#14/#15) all **MERGED to master**; master CI green (4/4 incl. real-Postgres RLS). **No active feature branch.** All work = PRs; never push to master directly.
- **Loop per commit:** implement-scope → gates (`check`/`check-types`/`test` + coverage gate + real-Postgres RLS where relevant) → codex adversarial review (CRITICAL/HIGH only) → fix → commit → push → update PR → lessons + PROGRESS.
- **Standing build order (Phase 2 onward):** schema → migrations → RLS → **ROBUST seed** → services → routers → validation → RBAC → audit/outbox → tests → API contract docs. **No production UI** until APIs stable+approved; later UI strictly from `ui-inventory/` + MCP registries (never hand-rolled React). POS UI is a **Phase 4** decision (Tauri/offline/SQLite) — not decided now.
- **Current step:** **Phase 3 — COMMIT 3 IMPLEMENTED (transfer VALUE conservation); STOPPED for owner review before merge (touches frozen costing → Codex handoff).** Branch `phase-3-commit-3` off `dbe77e2`. See the RESUME-HERE block at the top for the full commit-3 summary (additive `applyTransferInValuation`, the UNIQUE-constraint resolution, the turbo CI-gate fix, db 48/48 + api 17/17). **(prior, MERGED):** Commit 2 (transfer model + quantity conservation). Commits 0+1 MERGED to master (PR #13 `226d255`, PR #14 `767fd53`, both CI-green). Commit 2 on branch `phase-3-commit-2` (off `767fd53`). **Commit 2 (migration 0014):** new tenant-owned `stock_transfer` + `stock_transfer_line` (ENABLE+FORCE RLS; coverage gate green). Status flow draft→shipped→received/cancelled; **two-step** ship (source→per-transfer in-transit node) + receive (node→dest) via the sole ledger mutator (`transfer_out`/`transfer_in` movement types) → **quantity conserved at every step**; **intra-company-only** enforced by composite FKs `(tenant_id, company_id, location_id) → location` on all three endpoints (+ a service guard); date seams; transfer events created/dispatched/received/cancelled (value fields RESERVED null until commit 3). **NOTE:** commit 2 moves LEDGER QUANTITY only — `applyValuation` is NOT called, so `avg_cost`/`valuation_layer` intentionally lag until **commit 3** wires VALUE conservation (no consumer/UI yet; documented, NOT the #8 bug). Per-invariant write-path gate: the transfer router invokes `transfer.ts`; tests prove qty conservation, inter-company + cross-tenant rejection (router + raw DB composite FK), no-double-receive, no-cancel-after-received. Gates: check 162/0 · check-types 6/6 · test 7/7 · mojibake clean · DB-gated migrate 0000–0014 + db **45/45** (regression) + api **16/16** (+2 commit-2 tests) · coverage gate green. **Next: commit 3 (transfer VALUE conservation — the one frozen-costing additive touch; HARD GATE: existing Phase-2 costing suites must pass UNCHANGED) — awaits owner GO.** **STILL PARKED elsewhere:** #8 + `cost_reconciliation` (Phase 4); #6 + `valuation_updated` enrichment (Phase 5).
- **(prior, MERGED to master):** Commit 0 — parked debt #5 composite-FK + #7 set-once trigger (PR #13, `226d255`). Commit 1 — unified self-referential location model: type enum/CHECK, `parent_location_id` composite self-FK (child shares tenant+company), flags, capacity seam (PR #14, `767fd53`). Plan + Codex resolutions (PR #11, `211edaa`). Full detail in the changelog below.

### ⛔ BLOCKERS / gates

**Phase 2 backend is review-ready for the approved scope.** All product-policy decisions D1–D7 are LOCKED (below); only D-money rounding stays open (Phase 5, not a Phase-2 blocker — AVCO carries exact-integer remainders, FIFO is division-free; see the plan's *Value-integrity invariants*). Phase-2 schema, rich seed data, valuation/inventory services, inventory routers/reports, catalog CRUD, product Phase-2 create/list/update/archive, variant lifecycle, lot lifecycle, reorder-rule CRUD, discrepancy review, revaluation, import preview, mixed-catalog router e2e, API contract docs, gap analysis, and closeout audit now exist. **No P0 backend gaps remain for approved Phase-2 scope.** UI is still intentionally absent.

**✅ Owner decisions LOCKED (2026-06-22) — all in `module-specs/inventory.md`:**
1. **D1 costing** — AVCO default; FIFO per tenant/category/product (pharmacy/expiry/lot/regulated); no LIFO; not hardcoded — per-tenant/category/product strategy (both `avg_cost` + `valuation_layer` exist). + Costing Strategy Examples section.
2. **D2 multi-UoM** — canonical base units; integer-ratio conversion factors where possible; purchase/stock/sale/reporting units; conversions tenant/category/product configurable.
3. **D3 serial/batch/lot** — model all three (serial + batch/lot + expiry); ship lot/batch+expiry first; serial stubbed but schema must not block it.
4. **D4 expiry/FEFO** — NOT a global hard-block; tenant/category/product configurable; general-retail default warn-and-override (audited `inventory.override_expiry`); pharmacy/regulated/controlled hard-block selectable per category/product.
5. **D5 oversell** — allow-oversell-with-flagging default (negative ledger + `inventory.stock_discrepancy`); hard-block configurable per tenant/category/product. Ledger policy-neutral.
6. **D6 barcode** — data-driven parser config; GS1/EAN/UPC/Code128 + variable-measure/weight-embedded; conservative Phase-2 build (table + config seam; live scale/parser deferred to Phase 4).
7. **D7 reorder** — fixed min/max; suggestions only; no auto-PO; manager approval required before procurement.

**⏳ Still OPEN (only one):**
- **D-money rounding mode** — left OPEN per directive; do NOT assume banker's or half-up; pending Guyana/GRA VAT + target-country rounding verification. First needed Phase 5 (tax/FX division), not Phase 2.

**What I did:** finished + locked all D1–D7 in the §42 spec; added Costing Strategy Examples; kept rounding open; drafted the approved Phase-2 implementation plan, event map, inventory screen map, Commit-0 RLS coverage gate, Commit-1 catalog schema/migration/RLS, Commit-2 tracking schema/migration/RLS, Commit-3 costing storage/migration/RLS, Commit-4 reorder/count/BOM schema/migration/RLS, Commit-5 raw seed foundation (multi-tenant AVCO/FIFO/mixed catalog, UoM, varied lots, serial stub, reorder/count/BOM config), Commit-6 costing resolver/applyValuation service (product→category→tenant, AVCO remainder carry/zeroing, FIFO `FOR UPDATE` layer consumption), Commit-7 valuation-bearing seed movements (receipts/issues/oversell row through resolver, idempotency-key guarded), Commit-8 inventory services (UoM, FEFO, oversell, reorder), Commit-9 stock-count posting (count lines → valued adjustment movements, positive variance requires explicit value triplet), Commit-10 routers/reports (`inventory.receive` SKU-aware, adjust, count start/line/post, reorder evaluate, valuation + low-stock reports), Commit-11 API contract doc, Commit-12 catalog create routes, and Commit-13 gap audit/guard regression. **No UI yet.**

### §45 phase reassessment (end of Phase 1 / VS#1)
- Architecture held: fail-closed RLS + 3-role model + tenant-scoped `withTenant` is the load-bearing spine; every later module inherits it. No ADR changes needed. Codex found real HIGHs at the service/router layers (idempotency race, FK-bypasses-RLS, money int4) — all fixed + regression-tested; 0 CRITICAL across the whole slice.
- Tech debt queued (non-blocking): deferred §43 gates (a11y/Trivy/dep-audit/SAST/bundle/VRT), §44 perf gate, distributed number allocator, accounting GL (Phase 5), read-models (Phase 12).
- **Backlog — named future capabilities:**
  - **Exchange Settlement Engine** (owner-named 2026-06-24; the deferred `pos.exchange`). NOT "exchange-the-feature" — it is a coherent settlement subsystem to be built AFTER the stored-value/store-credit seam: net-difference settlement (pay only the difference) · store credit · gift cards · split tenders · excess-credit-as-store-credit · liability posting · commission reversal · loyalty reversal · (later) multi-currency settlement. Phase-4 Commit 3 ships Returns/Refunds/Voids and leaves the exchange **contract** locked (`event-map-phase4.md` decomposition = linked `sale.refunded`+`sale.created` sharing `exchangeGroupId`) with the `sale.exchange_group_id` column + reserved-nullable event field in place, so the producer is purely additive when built.
  - **`ui-registry.md`** (per-SCREEN one-line `Screen | Sources` sheet — e.g. `POS | AdminCN + Studio Checkout + Studio Cart + Studio Product Grid`). Owner directive 2026-06-24: **LATER, NOT now** — create it when UI work actually begins so a UI session never re-derives sourcing per screen. The per-**module** UI Source Registry already lives in `frontend-strategy.md` (PR #27 merged); `ui-registry.md` is the finer per-screen sheet on top of it.
  - **`product-platform-roadmap.md`** (owner-recorded 2026-06-27; docs branch `docs/product-platform-roadmap`) — authoritative record of currently-understood FUTURE directions, all recording-only / build-nothing: (1) **White Label & Branding** future capability on the `tenant_ui_config` seam — GREENFIELD; (2) **Module Entitlements** ⚠️ DESIGN-FIRST (dependency graph → valid bundles → absent-module behaviour, server-side enforced; output is an owner-gated DESIGN DOC, not a PR) — GREENFIELD, RBAC exists but tenant licensing does not; (3) **Product Packages** commercial planning (distinct layer below bundles); (4) **Marketplace / Extensions** long-term direction (record only); (5) **Deployment Profiles** operational models (single/multi-store cloud, self-hosted, MSP-managed, offline-first, enterprise HA) — orthogonal to packages/entitlements. Verified recon: both seams GREENFIELD (no merged-but-unwired surprise). **Components stay token-based, never module-aware** until the §2 design lands. Constitution updated: `engineering-principles.md` **#19 One Writer Per Working Tree** + **#20 Action Availability Rule** (backend decides actions, FE renders, BE enforces; UI visibility is never security — governs the upcoming sale-detail `availableActions` read). **🔒 Governance freezes after this docs PR** — no further roadmap/constitution/principles docs unless implementation uncovers a genuinely new architectural category.

### 📋 Deferred-decisions log (business/product rules I refused to guess)
1. **Monetary rounding mode** (half-up / half-even / half-away-from-zero) — charter §19 mandates "one rounding policy" but doesn't pick one. **STILL OPEN (owner directive 2026-06-22):** do NOT assume banker's or half-up — pending verification of the Guyana/GRA VAT rounding rule + other target-country tax rules. **Not needed for VS#1/Phase 2** (no division yet); first needed at **division**: tax (Phase 5) and FX. *(No work blocked now.)*
2. ~~**Oversell policy**~~ → **RESOLVED 2026-06-22 (owner directive):** "Allow Oversell with Flagging" default (ledger may go negative, emit `inventory.stock_discrepancy` for manager review); hard-block configurable per tenant/category/product (serialized/controlled/regulated/high-risk). `StockLedger.append` stays **policy-neutral** (as VS#1 shipped); the oversell policy resolver sits ABOVE the ledger. Recorded in `module-specs/inventory.md` D5.
3. **Inventory costing (D1)** → **RESOLVED 2026-06-22 (owner directive):** AVCO default, FIFO per tenant/category/product, no LIFO, not hardcoded (per-tenant/category/product strategy). Recorded in `module-specs/inventory.md` D1.
4. **D2/D3/D4/D6/D7** → **ALL RESOLVED 2026-06-22 (owner directive):** UoM (canonical base + integer ratios + 4 unit roles + configurable), serial/batch/lot (all three modelled, lot first, serial stubbed), expiry/FEFO (configurable, no global hard-block, audited override), barcode (data-driven config, conservative build), reorder (fixed min/max, suggest-only, manager approval). All in `module-specs/inventory.md`.
- *Still open (Phase 5):* monetary rounding mode only.

### ✅ PRs merged
- **PR #1** — `vs1-phase1` → master — **MERGED** (VS#1 complete).
- **PR #3** — `phase-2-inventory` → master — **MERGED** (Phase-2 §41/§42 docs + ADR-0007 after PR #2 auto-closed).

### 🚧 Active review branch
- **`phase-2-implementation`** — Commit 0 coverage gate + docs-only planning artifacts; Commit 1 catalog schema/migration/RLS; Commit 2 tracking schema/migration/RLS; Commit 3 costing storage/migration/RLS; Commit 4 reorder/count/BOM schema/migration/RLS; Commit 5 raw rich seed foundation; Commit 6 SKU ledger seam + costing resolver/applyValuation; Commit 7 valuation-bearing seed movements; Commit 8 UoM/FEFO/oversell/reorder services; Commit 9 stock-count posting service; Commit 10 inventory routers/reports; Commit 11 API contract doc; Commit 12 catalog create routers; Commit 13 gap audit + catalog FK regression; Commit 14 product Phase-2 create fields + mixed-catalog router e2e; Commit 15 catalog/product list routes; Commit 16 catalog/product/variant/lot/reorder lifecycle routes; Commit 17 discrepancy review, revaluation, import preview; closeout audit. UI is not implemented yet. Next: owner review, then P1 hardening or Phase 3 backend planning.

---

- **Current effort:** Phase 0 — **post-lock-in hardening**: audit-only red-team pass of Phase 0 vs charter v4.1 → fix CRITICAL findings only → then Phase 1 (Vertical Slice #1). Charter §0/§1/§32/§45/§47.
- **Approved plan:** `~/.claude/plans/retailos-master-architecture-groovy-oasis.md` (Deliverables A–E DONE + merged to `master`).
- **Working branch:** `master` (Phase-0 branch merged; commits `4ce9499`/`cfcf588`/`bdc51f6`). Foundation docs + config locked-in and CI green.
- **Charter:** `docs/architecture/retailos-master-charter.md` (v4.1).

## How to resume (any agent / after compaction)

1. Read `docs/architecture/lessons-learned.md`, this file, and the relevant charter section(s).
2. Check the **Work lanes** table, then the Task Board, for the next unclaimed/unchecked item.
3. **Claim a lane before writing** (add your session tag in the table) so parallel agents don't collide on the same file.
4. Keep commits small and grouped by deliverable; update this file's Task Board + Changelog in the same commit.
5. `git pull --rebase` before committing (multiple agents share this branch).

## Work lanes (multi-agent partition — claim before writing)

| Lane | Owner | Status |
|---|---|---|
| Deliverable A–E (charter, doc set, slice-1 design, foundation config, verify+merge) | done | ☑ done — merged to `master`, CI green |
| CI green + Docker optimization (2.38GB→168MB) + dep alignment + ADRs 0004/0005 | done | ☑ done (`cfcf588`/`bdc51f6`) |
| **Audit-only red-team pass (Phase 0 vs charter v4.1)** | this session | ◐ 5 parallel read-only audit agents running |
| Fix CRITICAL audit findings only; queue rest as backlog | this session | ☐ blocked on audit results |
| Phase 1 — Vertical Slice #1 implementation (from `vertical-slice-1.md`) | _unclaimed_ | ☐ after critical fixes |

## Task Board

Legend: ☐ todo · ◐ in progress · ☑ done

### Deliverable A — Charter + governance scaffolding  ✅ DONE (commit 7566eef)
- ☑ Charter `retailos-master-charter.md` (1429 lines) · root `CLAUDE.md` (lean + linked) · `adr/` (template + 0001 base-ui, 0002 no-feature-code, 0003 baseline-stack) · `module-specs/README` · `glossary.md` · `phase-roadmap.md` · `competitive/README` · README `@magicui`/`@magicui-pro` split reconciled.
- ◐ A polish: add ADRs 0004+ for the remaining locked decisions if not yet covered (tenant isolation, offline conflict seam, money minor units, Outbox, webhook dispatcher seam, fiscalization/OCR seams, search interface).

### Automation & coordination (NEW — user request)  ✅ DONE [lane: agent-infra]
- ☑ `SessionStart` hook (`.claude/settings.json`) surfaces a lean PROGRESS view to every session/agent (context linked to docs across compaction). Activates on next `/hooks` open or restart (settings file was created mid-session).
- ☑ "Progress protocol" + Work-lanes + subagent/parallel guidance in root `CLAUDE.md` (kept lean + linked).
- ☑ `scripts/log-progress.sh` — `show` (lean state for the hook) + `log "msg"` (stamp a dated changelog entry). Verified.

### Deliverable B — Architecture-review doc set  ✅ DONE (all 13 docs committed + merged to master)
- ☑ architecture-review · domain-model · auth-authz · tenancy-deployment · offline-edge-hardware · money-fiscal-inventory · accounting-crm-ecommerce · platform-saas-integrations · quality-security-ops · folder-structure-conventions · ui-ux-plan · security-baseline · phase-0-checklist. (Fidelity to be re-verified by the audit pass below.)

### Deliverable C — Vertical Slice #1 design  ✅ DONE
- ☑ `docs/architecture/vertical-slice-1.md` (§32 flow, schema, oRPC routers, tenant-guard middleware, RLS, util interfaces, test list). DESIGN ONLY — no code.

### Deliverable D — Phase-0 foundation config  ✅ DONE + GREEN
- ☑ Root scripts (`test`/`lint`/`check`/`check-types`/`build`) + turbo `test` task; husky + lint-staged; vitest (happy-dom) + smoke tests; playwright config + placeholder e2e; `.github/workflows/ci.yml` (quality+docker+e2e jobs, GHA cache, ≤350MB size gate, checkout@v5); docker-compose +redis +minio +`.dockerignore`; `.env.example` placeholders.

### Deliverable E — Lessons-learned + verify + merge  ✅ DONE
- ☑ lessons-learned entries appended (charter committed; base-lyra=Base UI; fumadocs shiki-WASM; distroless Bun-vs-Node). §46 gates verified green; CI green on master; branch merged.

### Phase-0 hardening (post-lock-in — current task list)
- ☑ #13 Fix red CI on master (fumadocs shiki-WASM excluded from build gate)
- ☑ #14 Full docker build locally (web image 2.38GB→168MB, boots+listens)
- ☑ #15 tech-stack.md dep follow-ups (lucide-react/biome dedup; better-auth+expo 1.6.20); gates green post-bump
- ☑ #17 ADR-0004 (central-infra reuse) + ADR-0005 (Product Intelligence Layer deferred)
- ☑ #20 heimdallone-v2 Docker optimization studied + applied (multi-stage/cache/distroless)
- ☑ #16 Audit-only red-team pass — 5 parallel read-only agents → **0 CRITICAL / 0 HIGH**; report in `phase-0-audit.md`
- ☑ #18 No CRITICAL to fix; resolved 5 stale-metadata MEDIUM contradictions (§40); dismissed 1 (postgres-18 volume, verified); queued engineering items
- ◐ #19 Begin Phase 1 — implement Vertical Slice #1 from `vertical-slice-1.md` (NEXT)
- ☐ Full Phase-0 completion report to user (requested)

## Verified facts (do not re-derive)

- **`base-lyra` IS the Base UI primitive family** — `@base-ui/react ^1.0.0` is in `packages/ui/package.json` and components import `@base-ui`. Satisfies charter §5 (no Radix/Base UI conflict).
- Existing root scripts: `check-types` (turbo) and `check` (ultracite) exist; `db:push/studio/generate/migrate` exist. **Missing:** `test`, `lint`, husky, lint-staged, vitest, playwright, happy-dom/jsdom.
- Registry config (root + `packages/ui` `components.json`) is live-verified and correct: `@shadcn`, `@magicui` (free), `@magicui-pro` (Bearer `${MAGICUI_PRO_REGISTRY_TOKEN}`), `@shadcn-studio`/`@ss-blocks`/`@ss-components`/`@ss-themes` (style-param `…/r/{style}/{name}.json` + `email`/`license_key` params), `@reui` (`base-nova`). Credentials in gitignored `.env`. See `ui-inventory/INDEX.md` + `lessons-learned.md`.
- UI inventory (Phase-0 §5 discovery) is COMPLETE: `ui-inventory/` 10 files; counts live-verified (shadcn core 414, studio 735/61, Magic UI free 245/77 + Pro 103, ReUI 17). Origin UI + shadcnblocks evaluated, not configured.
- Scaffold reality: Better Auth = email/password + Expo plugin only; DB = auth schema only, no migrations; 2 demo oRPC procedures; docker-compose = postgres + web only. All charter foundation domain work (tenant/RBAC/audit/RLS/Redis/object storage/Better Auth plugins) is NOT yet built (deferred past Phase-0 lock-in).

## Changelog (newest first)

- **2026-06-28** — PR #46 vet: Codex review found 2 HIGH (0 CRIT) — folded both: soft-delete guard on product.imageCreate parent select + objectKey scrubbed from imageCreate write DTO. Locked with regressions (archive→NOT_FOUND, no objectKey in response). Verified disposable PG18: api 53/53, db 93/93, zero skips.

- **2026-06-28** — Commerce UI direction LOCKED (CommerceO back-office + Shopix storefront, commerce-ui-plan.md). First build=CommerceO-skin Dashboard+Products (zero backend); nav stays AdminCN sidebar (tenant_ui_config option deferred). Codex handoff written (codex-handoff.md). PR #45 open (login+theme+radius+dashboardSummary+product.catalog), awaiting owner merge.

- **2026-06-25** — **Frontend constitution evolved to the RetailOS UI Platform (7-layer) — MERGED (PR #30; master CI green).** Supersedes the #27 5-layer stack: **AdminCN** shell → **CommerceO** commerce workflows → shadcn **Studio Blocks** → **Studio Components** (default primitive library) → **shadcn/ui** fallback → **Magic UI** motion → **RetailOS custom**. Reframed as a **UI Platform** — the templates are a *design-system source* that disappears into `packages/ui`; the win condition is that nobody can identify a component's origin (all re-themed to RetailOS tokens, normalized to Base UI, wired to oRPC). **NEW governance docs:** `ui-source-registry.md` (per-module Primary/Secondary/Studio/Custom + **Screen Composition Matrix**), `component-preference-matrix.md` (per-component preferred source — "which button do I use?"), `vertical-presets.md` (platform-not-product onboarding presets: General Retail/Pharmacy/Wholesale/Restaurant/Service/Custom — **config-only, NOT built**, wizard is Phase 11). Design-language skill gained the **8 core design principles**; `CLAUDE.md`/`AGENTS.md` synced. **Grounded in verified ZIP facts** (AdminCN + CommerceO both Base UI / `base-vega` / Tailwind v4 / Next 16, **zero Radix, zero bundled auth**); Pro templates gitignored, never forked, never committed. **Codex gate found 5 HIGH docs-consistency issues (0 CRIT) — all folded + confirm-verified** (auth-not-bundled facts vs "Clerk default"; command-palette + number-ticker cross-doc source conflicts between registry and component matrix; broken `AGENTS.md §35`/`(§6)` section refs; platform-first-principle reference). Docs-only; **no UI built** (no production UI until APIs stable + approved). See `frontend-strategy.md` §1. — lesson recorded: *cross-doc consistency is a real failure mode; one authoritative owner per concern; verify template facts from ZIP/MCP not marketing; Codex gates earn their keep on governance docs too.*
- **2026-06-24** — **Phase 4 Commit 3 (Returns/Refunds/Voids) MERGED (PR #26, `1aff83b`) + frontend governance MERGED (PR #27) + 🔒 DEVELOPMENT LOOP FROZEN.** Both PRs 4/4 CI-green, owner-approved, merged to master (`9307ef1`). **PR #26:** `pos.refund`/`pos.void` on MSP primitives; restock is #8-correct (valuation via `applyTransferInValuation`, value derived from the original stamped COGS, never `applyValuation` on the restock leg); Codex gate found 3 HIGH → folded 2 invariant bugs (HIGH-1 intra-request duplicate `originalSaleLineId` over-refund → aggregate-before-check; HIGH-2 sequential partial-refund rounding drift → cumulative-difference split, Σ = exactly original `cogsMinor`) + confirm-gate CLEAN; HIGH-3 (exchange net settlement) was a feature boundary → `pos.exchange` DEFERRED to the **Exchange Settlement Engine** backlog (contract + `exchange_group_id` seam reserved). db 83 + api 29, zero skips; frozen costing byte-identical. **PR #27:** frontend 5-layer stack (AdminCN shell → Studio blocks+variants → shadcn primitives → Magic UI → custom) + per-module UI Source Registry + Studio-is-first-class-variant-source; design-language skill updated. **Process freeze (owner directive):** the **RetailOS Development Loop** (12 steps: locked decision → small commit → local gates → real-PG → Codex review → fix CRIT/HIGH only → re-verify → Codex confirm gate → PR → human review → merge → update PROGRESS+lessons) is now canonical+FROZEN in `engineering-principles.md` (Group E), referenced from `CLAUDE.md`; implementation PR summaries now carry **Customer-visible capability delivered + Frontend surfaces unlocked + Backend services reused**; "ship the smallest valuable thing / first-customer drives the slice" added to Group F. **NEXT:** Commit 4 chosen by FIRST-CUSTOMER need (not blind phase order) — awaiting the owner's pilot-customer profile. `ui-registry.md` (per-screen sheet) deferred to when UI work begins.
- **2026-06-25** — **Phase 4 — Commit 4 (Configurable Cash Control) — Codex gate FOLDED (5 HIGH, 0 CRIT) + MERGED (PR #29).** The generic retail cash-control spine — **Shift · Cash Drawer · Blind Close · Over/Short · X/Z** — built **configurable** per the platform-first principle (default broadest, specialize through config, NEVER an architecture fork). **Three toggles via the settings resolver** (`shift_enforcement` required/optional/disabled default optional · `blind_close` on/off · `cash_drawer` on/off), resolved location → tenant → platform — NO business-type branch; columns on `organization`+`location`. **New tenant tables** `shift` + `cash_movement` (fail-closed RLS hand-added; coverage gate green; H1 composite FKs; partial unique index = one open shift/terminal). **APIs:** `pos.openShift` (multi-currency float) · `pos.cashMovement` (pay_in/out/drop) · `pos.closeShift` (**BLIND** — input carries only countedCash; system computes `expected = Σfloat + cash-sales-settled + pay_in − pay_out − drop` and `overShort = counted − expected`, per currency) · `pos.xReport`/`pos.zReport`. **`loadShiftForUpdate` = `SELECT … FOR UPDATE`** before the status guard (Commit-3 race lesson) → concurrent closes serialize (proven). `createSale` wires `sale.shift_id` per `shift_enforcement`. Events `shift.opened`/`shift.closed` to the locked event-map (per-row functional twins reserved-null; P5 posts per posting-model §7/§8). RBAC `pos.open_shift`/`pos.cash_movement`/`pos.close_shift` (cashier+manager+admin). **Verified real PG18 (disposable DB):** chain 0000→0020 applies; **db 83/83 + api 34/34, zero skips** (+5 shift tests: one-open-per-terminal, blind over/short EXACT 13500/−500, toggle disabled/required, close race one-winner, H1 cross-tenant); check-types + lint + mojibake green; **frozen `costing.ts`/`costing.rls.test.ts` byte-identical**. Codex gate found 5 HIGH (0 CRIT) — all folded + confirm-verified (cash refunds/voids reconciled into drawer expected cash per (currency,scale); sale→shift terminal-ownership; cashMovement/closeShift terminal guard; toggle CHECK constraints; DB-resolved shiftId on events) + 6 regressions; merged after #28/#30 with PROGRESS rebased.
- **2026-06-25** — **Phase 4 — Commit 5 (Payments Maturity + Receipt/Fiscal Readiness) BUILT on `phase-4-commit-5-payments-receipts`; awaiting Codex gate + PR (NOT merged).** Backend-only, no schema/migration/UI/GL/offline/fiscal-provider. Tender maturity now explicitly allows configured settlement methods (`cash`, `card`, `mobile_money`, `bank_transfer`) and blocks stored-value methods (`store_credit`/`gift_card`) as the voucher/store-credit seam until the liability ledger lands; underpayment and cash-change rules remain enforced. `payment.received` keeps the Phase-4 contract fields and adds the requested accounting-consumer aliases (`currency`, `scale`, `fxRateUsed`, `tenderType`, `sourceType/sourceId`, `terminalId`). Added versioned (`schemaVersion: 1`, `receiptVersion: 1`) `buildSaleReceipt` backend receipt read model + `pos.receipt` route so the frontend renders authoritative backend totals/payments/line/fiscal placeholders instead of recomputing; receipt number reuses stable sale/fiscal seams, invoice number reuses the existing invoice allocator path, and no parallel numbering system was created. Tests extend the MSP flow for receipt totals, cash overpayment/change, same-currency multi-tender, stored-value seam rejection on sale + refund, payment-event completeness, and tenant-isolated receipt reads. Verified on central-style `retailos-overnight` PG18: Drizzle migrate clean on fresh `retailos_commit5_drizzle`; DB-gated `bun run test` against `retailos_commit5_receipts` = **db 83/83 + api 40/40, zero skips**; check-types + lint + mojibake green. **Follow-up debt:** extract receipt read models out of `packages/db` before Phase 12 reporting/application read-model expansion. STOP next for Codex adversarial review.

- **2026-06-24** — **Phase 4 — Commit 3 scoped to Returns / Refunds / Voids; `pos.exchange` DEFERRED (owner decision); awaiting Codex confirm-gate + PR (NOT merged).** After the Codex gate's HIGH-3 (exchange settles the new sale in full and never applies the return credit), the owner chose to **defer `pos.exchange` to a later commit** — net-difference settlement (pay only the difference; excess return credit becomes **store credit**, not cash) needs the stored-value seam. **REMOVED** from `vs1.ts`: the `exchange` procedure, `runExchange`, the exchange integration test, the now-dead `exchangeGroupId` parameter + exchange-leg tender branch in `runRefund` (a standalone refund always disburses `tenders == refund amount`), and the exchange-only `newLineSchema`. **KEPT as reserved seams** (expand-only, additive-when-built): the `sale.exchange_group_id` column + `sale_line.original_sale_line_id` (actively used by refunds), the reserved-nullable `exchangeGroupId` field on `sale.created`/`sale.refunded` (emitted null), and the `saleType` enum's `"exchange"` value. The exchange **contract** stays locked in `event-map-phase4.md` (only the producer is deferred — `PRODUCER STATUS` note added). Docs aligned: dependency-map title/§2/§7 + event-map. **Re-verified:** check-types + lint clean (no orphaned symbols); real PG18 chain 0000→0019 applies; **db 83/83 + api 29/29, zero skips** (exchange test removed; all refund/void/over-refund/rounding/H1/race tests pass); **frozen `costing.ts`/`costing.rls.test.ts` byte-identical**. STOP — Codex confirm-gate next, then PR (Returns/Refunds/Voids), exchange listed as deferred.
- **2026-06-24** — **Phase 4 — Commit 3 (Returns) CODEX GATE FOLDED (0 CRIT, 3 HIGH) on `phase-4-commit3-returns`; awaiting confirm-gate + PR (NOT merged).** Adversarial `codex:codex-rescue` review of the built commit found **0 CRITICAL, 3 HIGH** and verified the two flagged areas CLEAN (the FIFO `qtyDelta:0` companion-anchor is replay-safe — `appendStockMovement` does NOT dedupe on `idempotencyKey`, the boundary is `runIdempotent`; the row-negative/event-positive sign convention nets correctly in `reports.salesBasic`). **Two HIGHs were decision-free invariant bugs — FOLDED:** (HIGH-1) intra-request duplicate `originalSaleLineId` bypassed the over-refund guard (two `{L,qty:1}` entries each saw the same `already=0` and both passed → double-restock) → now **aggregate requested qty per original line BEFORE the check**, one return line + one restock per original; (HIGH-2) sequential partial refunds rounded each independently against the original total (`mulDivRound(101,1,3)=34` ×3 = 102 ≠ 101) → now **cumulative-difference split** (`after − before` cumulative targets) so Σ across all partials = EXACTLY the original stamped `cogsMinor` (34+33+34=101) — same exact-conservation pattern as the Phase-3 bond-release F3 split. **The third HIGH (exchange net settlement — the new sale leg requires full tender, the return credit isn't applied) is a genuine business-rule boundary → surfaced to the owner, NOT guessed** (the locked Phase-4 contract deferred net settlement to P5). **+2 regression tests** (HIGH-1 duplicate rejected + legit aggregation = 1 line; HIGH-2 three sequential refunds restock exactly 101, AVCO cell ends qty3/value101). **Re-verified real PG18:** chain 0000→0019 applies; **db 83/83 + api 30/30 as `retailos_app`, zero skips**; **frozen `costing.ts`/`costing.rls.test.ts` byte-identical**. Gates check-types/lint/mojibake green. STOP — awaiting owner decision on the exchange HIGH + a Codex confirm-gate before PR.
- **2026-06-24** — **Phase 4 — Commit 2 (MSP) MERGED to master (PR #25, `a02e60b`; CI 4/4 green, Codex CLEAN). Commit 3 (Returns/Refunds/Voids/Exchanges) BUILT on `phase-4-commit3-returns`; awaiting Codex gate + PR (NOT merged).** Owner approved + merged the MSP, authorized Commit 3 with a one-page reuse map (`phase-4-commit3-dependency-map.md`). Delivered (backend, expand-only): **EXTEND `sale`** (`exchange_group_id`) + **`sale_line`** (`original_sale_line_id` + `unique(tenant_id,id)` target + self-referential composite FK — H1 kill); added `SaleRefunded`/`SaleVoided` to `DomainEventType`; `pos.refund`/`pos.void_sale` RBAC (admin+manager, NOT cashier — §19/§22). **Three routers, building ON the MSP primitives:** `pos.refund` (first-class `saleType="return"`, NEGATIVE money so reports net; partial; per-line over-refund TOCTOU guard under `SELECT … FOR UPDATE`; emits `sale.refunded`), `pos.void` (full reversal of an un-refunded sale, `status="void"`, emits `sale.voided` parks-on-originalSaleId), `pos.exchange` (decomposes into a linked refund+sale sharing one `exchangeGroupId` — NOT a distinct event). **#8 discipline:** restock invokes valuation via the FROZEN value-exact lander `applyTransferInValuation` (lands EXACTLY the value the sale removed; FIFO remainder uses a qtyDelta:0 anchor — the transfer's two-movement trick); **`restockedValueMinor` DERIVED from the original line's stamped COGS proportional via `mulDivRound`** (event-map HIGH-4), NEVER from valuation on the restock leg. **Tests (the #8 write-path gate for returns):** router-driven refund actually MOVES `avg_cost` (AVCO 200→400) + `valuation_layer` (FIFO value-exact 67 across a 2-layer split), over-refund rejected, void full-restock + double-void race (one winner via FOR UPDATE), exchange linked pair, cross-tenant refund/void rejected (H1). **Verified real PG18:** chain 0000→0019 applies (FK-after-unique hand-reordered — Phase-3 lesson); **db 83/83 + api 28/28 as `retailos_app`, zero skips**; **frozen `costing.ts`/`costing.rls.test.ts` byte-identical** (reused the lander, never touched). Gates mojibake/check/check-types green; no new tenant table (coverage gate unaffected). **Frontend surfaces unlocked:** sale-detail Refund/Void actions, refund dialog (capped at original qty), exchange flow (return-cart + new-cart), credit-note/return-receipt preview. STOP for Codex gate + owner PR.
- **2026-06-24** — **Phase 4 — Commit 2 (Minimum Sellable POS) BUILT on `phase-4-msp`; awaiting Codex gate + PR (NOT merged).** Owner re-sequenced Phase 4 value-first (sale path before shift/cash — `shiftId` is reserved-nullable, so shift is NOT an architectural prerequisite) and merged tender+sale into ONE coherent MSP. Delivered (backend, expand-only): **EXTEND `sale`** (shift/rep/customer seams, subtotal/discount/tax, `sale_type`+CHECK, `original_sale_id` seam) + **`sale_line`** (sku/lot composite FKs, `qty_base`, line tax/discount, the **COGS stamp** `cogs_minor`/`cogs_currency`/`cogs_scale`/`costing_method_applied`); **NEW `tender` table** (cash/card/bank/mobile/cheque/store_credit/gift_card; fail-closed RLS, coverage gate + runtime fail-closed). Rewrote `pos.createSale`: sellable-location guard (INV-P4-7), SKU-required on tracked lines (INV-P4-8), **per-line `appendStockMovement` + `applyValuation` (#8 — closes CRITICAL-1: the POS write path now consumes FIFO layers / reduces AVCO value and STAMPS COGS on the line)**, tender settlement (underpayment rejected, cash change computed), invoice + gapless number, idempotent; emits the **extended `sale.created`** (functional/commission fields reserved present-but-null) + **`payment.received`** per tender. Added **`product.search`** (name/sku/barcode). **Tests:** the #8 write-path gate (router drives AVCO+FIFO sale → `avg_cost`/`valuation_layer` actually MOVE + COGS stamped), guards (sellable/SKU-required/underpayment), idempotency, cross-tenant SKU (H1). **Verified real PG18:** chain 0000→0018 applies clean as migrator; **db 83/83 + api 24/24 as `retailos_app`**; **frozen `costing.ts`/`costing.rls.test.ts` byte-identical** (reused `applyValuation`, never touched). Gates mojibake/check/check-types/test green. **Frontend surfaces unlocked:** POS checkout (product search → cart → tender → receipt), receipt/invoice view, sales list/detail. **Codex gate FOLDED (3 CRIT/HIGH + 1 MED):** (1) #8 recurrence — `skuId` now REQUIRED on every line + `applyValuation` UNCONDITIONAL (valuation cells are SKU×location; a product-only line had no cell → silently skipped); (2) change can no longer exceed cash tendered (cash-only, bounded; `tender` CHECKs `change/settled >= 0`); (3) `sale.created` now carries the locked reserved-nullable `offline` object; (4) `sale_type` made nullable+DEFAULT (no NOT-NULL retrofit — expand-only). Re-verified real PG18: chain 0000→0018 clean, **db 83/83 + api 24/24** (incl. the #8 gate, the no-skuId rejection, and card-overpay-tiny-cash rejection), costing byte-identical. STOP for owner PR.
- **2026-06-24** — **Phase 4 — Commit 1 (Foundation & schema seams) MERGED to master (PR #20; Codex adversarial review CLEAN: 0 CRITICAL/0 HIGH).** Owner authorized the build after the source-of-truth validation. Expand-only, no POS/payment/shift/commission/FX logic. Delivered: (1) **`mulDivRound(a,b,c,mode)`** BigInt precision primitive (`services/rounding.ts`) — exact intermediate math (>2^53 safe), `half_even` + `half_up`, **TDD: 13 tests incl. the mode-divergence ties (0.5/2.5/4.5) and negative/large-bigint cases**; (2) **VAT/TIN seams** — nullable `vat_registration_number` + `tax_identification_number` on `organization` AND `company`; (3) **`fiscal_document`** fiscalization seam (charter §17) — all provider fields nullable, doc-type/status `text`-enum+CHECK, composite FKs to company+sale (H1-safe), invoice as a documented bare-uuid seam, **fail-closed RLS (ENABLE+FORCE+tenant_isolation), coverage gate green + runtime fail-closed proven**; (4) **`number_block`** §17 scope expand — added `fiscal_year`; new finer `number_block_scoped_uq` (NULLS NOT DISTINCT) kept ALONGSIDE the legacy coarse unique (contract/drop = later migration); (5) **composite-FK `(tenant_id,id)` targets** on `sale` + `number_block`. Migration **0017** HAND-REORDERED (drizzle emitted the fiscal_document→sale FK before sale's UNIQUE target — the Phase-3 FK-before-unique lesson) + hand-added RLS DO-block. **Verified on real PG18:** full chain 0000→0017 applies cleanly as BOTH superuser AND the `retailos_migrator`→owner role; **DB-gated suite 83/83 as `retailos_app`** (incl. fiscal_document added to the runtime fail-closed list; frozen costing suite byte-identical/green). Repo gates: mojibake/check/check-types/test all green. **Codex review CLEAN (0 CRITICAL/0 HIGH); MERGED to master.** Next per owner: value-first Phase-4 execution re-sequence (Minimum Sellable POS) before Commit 2.
- **2026-06-24** — **UI governance track MERGED to master (#12 design skill → #21 AdminCN findings → #22 frontend strategy; master `675133a`, CI green) + added the ENGINEERING-PRINCIPLES constitution (docs-only).** Both governance tracks (backend architecture + frontend/design) are now mature and frozen. Created `docs/architecture/engineering-principles.md` — the contributor constitution graduating this project's recurring lessons into law: backend-owns-truth / frontend-owns-presentation, **one owner per invariant**, **every write path goes through the owning service (#8 class)**, Money primitives + `mulDivRound` for all division, **no UI does business calculations**, **stamp the irreproducible fact at txn time** (occurredAt/costing_method_applied/fxRateToFunctional/commission), imported components become **owned code** (never fork), verify external facts against official docs + a live probe, **track gate findings by KIND not count**, the per-phase finish loop (codex review → tests → CI-from-logs → PR → lessons+PROGRESS same commit), and the **products-not-phases** mental model. Owner-requested additions folded the same pass: **Invisible UX** expanded with the 4-question framework (what is this / why / what happens / can I undo) in the design skill; **Source Priority Matrix** (per-need source table) + **NEVER MODIFY IMPORTED BLOCKS DIRECTLY** (import→normalize→adapt→extend) + **build layers not pages** (Application Shell → Module Shell → Reusable Workflows → Blocks → Screens) in `frontend-strategy.md`. `CLAUDE.md`/`AGENTS.md` point to the constitution. **Verdict: design phase done — execution from here (≈80% implementation / 15% UX-as-APIs-stabilize / 5% architecture-only-on-new-problem-categories).** Docs-only, separate PR.
- **2026-06-24** — **Locked the official FRONTEND STRATEGY into governance (docs-only; before Phase-4 UI work).** Created `docs/architecture/frontend-strategy.md` — the binding sourcing & adaptation law: backend stays RetailOS-authoritative (TanStack Start/Hono/oRPC/Better Auth/Drizzle/PG/RLS/events); UI is **never rebuilt from scratch and AdminCN is never forked** — source in preferred order **shadcn Studio → AdminCN blocks/layouts → Magic UI → custom (`gaps-and-custom.md`)**; every imported block **becomes owned code in `packages/ui`, wired to oRPC, re-themed to RetailOS tokens, stripped of mock data + Next.js/auth/routing assumptions**; **AdminCN = visual target, backend = authoritative**. Restates the visual system (blue accent, rounded, light+dark, density modes, semantic badges, right-aligned monospace financials, progressive disclosure), required views (Table/Card/Detail/Timeline + optional Calendar/Kanban/Map/Hierarchy), UX rules (tooltips/helper-text/empty-states/validation/Cmd-K/keyboard), offline UX (Synced/Syncing/Queued/Failed, icon+text), and build sequencing (backend first; UI after APIs stabilize → import→adapt→wire→parity). **Defers (no duplication) to the `retailos-design-language` skill (PR #12) for the design law and `ui-admin-shell-findings.md` (PR #21) for AdminCN detail.** Updated `CLAUDE.md` + `AGENTS.md` (kept in sync) with a governance pointer. Gates: mojibake/check-types green. Docs-only, separate PR (`docs/frontend-strategy`).
- **2026-06-24** — **Phase 4 — created authoritative `posting-model.md` (source-of-truth) + validated `event-map-phase4.md` against it = CLEAN (accounting-complete); Commit 1 STILL not started, awaiting owner build authorization.** Root cause of the two structural-dimension gates (functional-currency, commission): no single authoritative posting spec — every reviewer reconstructed the GL's journal impact from scattered docs. Fix (owner-directed): wrote **`posting-model.md`** — the authoritative enumeration of every GL journal RetailOS posts per transaction (sale/refund/void/payment/stored-value issue+redeem/shift open+close/inventory adjust+receipt/bond release), each with Dr/Cr accounts, posting timing, required event fields, reversal/FX/commission behavior, sourced from charter §17–§20 + locked decisions; 10 universal conventions (C1–C10: balance, functional currency, perpetual inventory, cash-clearing bridge, liability-not-revenue, idempotency…); §12 classifies out-of-scope dimensions (loyalty=P7, tips=restaurant, AP/landed-cost=P6) so the model is provably complete. **Then validated `event-map-phase4.md` against it transaction-by-transaction:** every posting-model journal-line amount for all 8 Phase-4 transactions is **carried or reserved-nullable** — CLEAN, no gap (incl. the subtle payment→clearing self-sufficiency: `clearing = settledFunctional − realizedFX`, derivable from the payment's own fields). **VERDICT: Phase 4 event contracts are accounting-complete and implementation-ready.** Updated event-map (validation table), `cross-phase-dependencies.md` (posting-model = source-of-truth-per-journal), roadmap (Phase 4 = contracts accounting-complete). Gates: mojibake/check-types/test green. **STOP — this was the final source-of-truth validation pass; Commit 1 NOT started in this pass (owner authorizes the build session).** No branch, no code.
- **2026-06-24** — **Phase 4 event-map GATE 4 (final consumer-completeness) — FOUND 2 HIGH classified NEW STRUCTURAL CATEGORY (commission) → did NOT point-fix; imposed a checkable invariant + re-derived the whole contract; Commit 1 STILL not started.** A 4th fresh `codex:codex-rescue` pass (6 events PASS — the functional-currency fix held) found the GL's **commission accrual** journal (Dr commission-expense/Cr commission-payable) had **no field on any event**, though the map even *referenced* "commission clawback" — a missing posting **dimension**, not a missing field. Per the owner's pre-committed decision tree (a *new structural category* ⇒ stop patching), I did **not** bolt commission on + re-gate. Instead I imposed two **checkable invariants** on the whole contract: **POST-1** (every monetary quantity = the full quintuple `*Minor`+`currency`+`scale`+`*FunctionalMinor`+FX-rate-used) and **POST-2** (the contract **enumerates every journal the GL posts per event** from charter §19/§20, and every enumerated amount travels on the event), then **re-derived** the contract under POST-2 — a per-event journal table that surfaced commission as the ONLY missing dimension and let me classify the rest as present/derivable/out-of-scope (discount posts net + `discountMinor` present; rounding residual verifiable; loyalty = P7; tips = restaurant). **Commission folded** (Decision #7 `commission_accrual_policy`: `at_sale` → fields on `sale.created`, clawback on refund [proportional, derived from the original line's stamp]/void [100% via original-journal reversal]; `at_settlement` → fields on `payment.received`; all reserved-nullable, stamped at the accrual trigger — 3rd "stamp the irreproducible fact" instance after `occurredAt`/`costing_method_applied`/`fxRateToFunctional`). Updated `event-map-phase4.md` (POST-1/POST-2 section + enumeration table + commission fields on 3 events), `cross-phase-dependencies.md`, `lessons-learned.md` (track findings by KIND not count; new structural kind ⇒ impose a checkable invariant + re-derive). Gates: mojibake/check-types/test green. **STOP — gate found HIGH (structural), so Phase-4 Commit 1 NOT started. The re-derived contract should get ONE validation gate (confirm POST-1/POST-2 closed all dimensions — NOT a point-fix-regate); if clean, contract LOCKED + Commit 1 authorized.** No branch, no code.
- **2026-06-24** — **Phase 4 event-map CONSUMER-COMPLETENESS GATE (3rd, owner-requested) — FOUND 7 HIGH (one systemic root cause) → folded; Commit 1 STILL not started.** A THIRD fresh `codex:codex-rescue` pass asked a DIFFERENT question than the prior two (which checked payload-completeness + symmetry): **can the Phase-5 GL build every balanced double-entry journal from each event's payload ALONE, with zero mutable-OLTP re-reads?** **PASS:** `sale.voided` (parks on + reverses the original journal — needs no amounts). **7 HIGH, all the same root cause:** every financial event carried transaction-currency amounts + the tender→sale FX rate (`fxRateToSale`) but **NOT the sale→FUNCTIONAL-currency rate** the books post in — forcing a mutable FX-table re-read at post time, which breaks deterministic replay (INV-P5-4 + INV-P5-7). **Verified real** against the Phase-5 invariants before folding (not inflation). **Fix (docs-only):** added reserved-nullable functional-currency context (`functionalCurrency`/`functionalScale`/server-stamped `fxRateToFunctional`) + a `*FunctionalMinor` twin beside every posting amount on all 7 financial events (per-row on multi-currency shift drawers; per-leg `cogsFunctionalMinor`/`settledFunctionalMinor`); refund REUSES the original sale's stamped rate (never re-quotes). Recorded the `company.functional_currency` seam + the **multi-currency sequencing constraint** (a historical FX rate can't be backfilled) in `cross-phase-dependencies.md`; cross-referenced INV-P5-4; appended the lesson (**consumer-completeness ≠ symmetry; stamp the irreproducible fact at txn time — 3rd instance after `occurredAt`/`costing_method_applied`**). **Single-currency (Guyana zero-config) tenants ship identity/null = zero setup burden.** Gates: mojibake/check/check-types/test green. **STOP — gate found HIGH, so Phase-4 Commit 1 NOT started; awaiting owner review + one clean re-gate.** No branch, no code.
- **2026-06-24** — **Phase 4 event-map RE-GATE — FOUND 1 HIGH + 1 MED + 1 LOW (asymmetries the FIRST fix pass introduced) → folded; Commit 1 STILL not started.** Second fresh `codex:codex-rescue` re-review of the corrected `event-map-phase4.md`. **PASS:** sale.created completeness; **sale.refunded FIFO-COGS correctness CONFIRMED** (deriving restocked value from the original sale line via `originalSaleLineId` is the correct AND only viable resolution — Codex verified `applyFifoReceipt`/`applyAvcoReceipt` return `cogsMinor:0` on restock). **GAPs folded:** (HIGH) `stored_value.issued` was missing `paymentId`/`tenderId` that `redeemed` had → added (nullable, symmetric); (MED) `exchangeGroupId` lived only in prose → added as an explicit nullable field in both `sale.created` + `sale.refunded` payloads so contract tests can bind it; (LOW) `cross-phase-dependencies.md` refund row said `cogsMinor` → aligned to `restockedValueMinor`. **Lesson:** a fix pass can introduce NEW asymmetries — a paired/reversal event must mirror its partner field-for-field; re-gate after folding. **Also created `event-versioning.md`** (version/additive-fields/deprecation/upcasting/replay policy — owner-requested before heavy Phase-4 coding) + added the **`inventory_class`** reserved seam (consignment/vendor-owned/customer-owned) to the cross-phase map. Gates: mojibake/check/check-types/test green. **STOP — re-gate found HIGH, so Phase-4 Commit 1 NOT started; awaiting owner review + one more clean re-gate.** No branch, no code.

- **2026-06-24** — **Phase 4 pre-build GATE: `event-map-phase4.md` adversarial review — FOUND 5 HIGH + 1 MED → folded; did NOT proceed to Commit 1 (docs-only; STOP for owner review).** A fresh `codex:codex-rescue` (never a fork) reviewed the 6 POS event contracts against the Phase-5 GL consumer. **Verdict: `sale.voided`/`payment.received`/`shift.opened`/`shift.closed` PASS; `sale.created`/`sale.refunded` GAP.** Folded all into `event-map-phase4.md`: (H1) COGS is now a full money triple `cogsMinor`+`cogsCurrency`+`cogsScale` on sale + refund (ADR-0008/INV-P5-4 — inventory cost basis can differ in currency from the sale); (H2) refund `taxBreakdown` locked to `{taxRateId,rate,baseMinor,taxMinor}` (was an ellipsis); (H3) refund tenders gained FX fields `tenderCurrency/tenderScale/fxRateToSale/settledAmountMinor`; (H4) refund `cogsMinor` → `restockedValueMinor` with a producer note — `applyValuation()` returns `cogsMinor:0` on a restock (positive) movement, so the return path **derives it from the original sale line's posted COGS** (via `originalSaleLineId`), reversing exactly what the sale posted; (H5) added **`stored_value.issued`/`stored_value.redeemed`** events (gift-card/store-credit LIABILITY posting — §19 — was a missing event class P5 needs) + to `DomainEventType`; (M6) **exchange = linked `sale.refunded`+`sale.created` sharing `exchangeGroupId`** (not a distinct event). `cross-phase-dependencies.md` updated (stored-value row). **Each gap would have forced a breaking event-contract change AFTER POS was built — caught before any code.** Also added the rounding **zero-config-default** guarantee to Decision #4 (Guyana invoices day-one with no rounding setup; config is the escape hatch, not a burden). Gates: mojibake/check/check-types/test green. **STOP — gate found HIGH gaps, so per the gate rule Commit 1 (POS schema seams) was NOT started; awaiting owner review of the contract fixes.** Branch: none created. No code.

- **2026-06-23** — **Phase 4 decision-lock pass + Phase 5/6 plan-hardening COMPLETE (docs-only; NOT a build authorization).** **TASK A (GRA rounding):** verified directly on `gra.gov.gy` (NOT Ghana) — GRA mandates NO rounding algorithm (7/57 VAT fraction + examples, no rule; VAT Act PDF image-scanned, no clause). ERP comparison (Xero/QBO per-line; Odoo/ERPNext configurable) → **Decision #4 evolved per owner redirect from "half-even global" to a CONFIGURABLE rounding-policy framework** (currency-scale + per-line/per-total granularity + mode, config-driven per country/currency; default per-line + half-even; `mulDivRound(a,b,c,mode)` parameterized). **TASK B (7 decisions LOCKED in `phase-4-implementation-plan.md` §12):** fiscalization none+`fiscal_document` seam; D5 oversell default; **int8** sale-line qty; configurable rounding; first-class returns; Tauri SQLite; **commission `commission_accrual_policy` enum (at_sale|at_settlement) with reversal semantics specified for BOTH modes** (at_sale: refund/void clawback; at_settlement: cancel reverses all accrual, forfeited deposit = non-commissionable). **TASK C/E (readiness):** verdict → **✅ Phase 4 planning COMPLETE & implementation-ready** (§15). **TASK D (sequential Codex reviews, fresh `codex:codex-rescue`, never forks):** Phase 5 GL — **3 CRIT + 4 HIGH + 3 MED** folded (CRIT: consumer-side idempotency INV-P5-7 `accounting_processed_event`; event ordering/parking INV-P5-8; inventory↔GL reconciliation depends on P4 #8); Phase 6 — **6 HIGH + 6 MED** folded (bond/GRN = shared primitive + SEPARATE wrappers not a flagged service; FIFO landed-cost layer policy; AP event seam; RLS/H1 tuple validation INV-P6-6). Each finding re-verified against the real schema before folding. **NEW DOCS:** `event-map-phase4.md` (6 events shaped for the P5 GL consumer — COGS/tax/tender/reversal IDs), `cross-phase-dependencies.md` (P2→P12 producer/consumer map + reserved future-proofing seams: stock_ledger source_type/source_id, location geodata, physical/logical class, reserved_qty/value, barcode type/value/parsing_profile), VAT-reg#/TIN seams + number_block expand/contract migration strategy added to the P4 plan. **STOP — no schema/migrations/code/branch/PR; locking decisions + hardening plans is NOT a build authorization (separate session).**

- **2026-06-23** — **Step 6: Phase 4/5/6 research + Codex plan-review COMPLETE (post-reset; supersedes the PART B "research blocked" note below).** Web research re-run **sequentially by the main agent** (no agent fan-out, per the rate-limit lesson) → 3 new sourced competitive docs with per-cell verification legends: `competitive/pos.md` (Lightspeed/Square/Shopify POS/Odoo + the **load-bearing Guyana-GRA fiscalization finding: NO fiscal-device/e-invoicing mandate → seam + "none" provider**, HIGH/documentary), `competitive/accounting.md` (QBO/Xero/Zoho/Sage; match Xero on multi-currency; Guyana VAT Form G0002/14%/due-21st; corp tax 40%/25%), `competitive/procurement.md` (Cin7/Fishbowl/Zoho/inFlow/Odoo; ASYCUDA/eSAD, CIF+CET-duty landed-cost basis, CET 5–20%). All `LIVE RESEARCH PENDING` banners replaced with verified findings + tax-expert caveats. **Codex adversarial plan-review (fresh `codex:codex-rescue`, NOT a fork) of the Phase-4 plan against the real schema/services: confirmed §2 grounding held; found 3 CRITICAL + 4 HIGH + 3 MEDIUM + 1 LOW — ALL folded into the plan** (each re-verified by me against the schema): #8 POS↔valuation close (INV-P4-3), sellable-location guard (INV-P4-7, `company.ts:70-73`), `#6 mulDivRound` is a P0 hard blocker not optional, composite-FK migration for POS tables (INV-P4-10), number_block location+fiscal-year scope (INV-P4-4), lease concurrency state-machine (§5), SKU-required (INV-P4-8), gift-card→stored-value-ledger downgrade, device-counter uniqueness (INV-P4-9), `fiscal_document` reserved-nullable schema seam (§8). Plan §14 records the review. **NO decision locked — the 7 Phase-4 (+5 P5 +5 P6) 🔒 decisions are surfaced WITH recommendations for Kareem to lock, Phase 4 first.** Docs-only (HARD GATE trivially holds); mojibake clean.

- **2026-06-23** — **PART B: Phase 4/5/6 PLANNING docs** (branch `phase-3-overnight`; planning only, NO code). `phase-4-implementation-plan.md` (DEEP, codebase+charter-grounded: POS sale/payment/tender, shift+cash/blind-close/X-Z, offline queue + conflict policy reusing D5, number-block reservation, fiscalization SEAM, RBAC, INV-P4-1..6, 7 🔒 decisions). `phase-5-implementation-plan.md` + `phase-6-implementation-plan.md` (SKELETONS: accounting as event-consumer/double-entry/#6-blocker; procurement reusing the valued-receipt engine + unify-with-bond-receipt + FIFO-landed-cost forces the Phase-2 OPEN decision). **⚠️ LIVE WEB RESEARCH BLOCKED** by an API session limit (resets 2pm Guyana) — competitive matrices + Guyana-GRA fiscal specifics marked "LIVE RESEARCH PENDING" with the exact agenda; per §40 nothing fabricated. Codex plan-review also blocked → self-reviewed for codebase/charter accuracy. **Re-run research + Codex plan-review before locking.** Detail in `OVERNIGHT-LOG.md`.

- **2026-06-23** — **Phase 3 COMMIT 7** (branch `phase-3-overnight`; overnight run, NOT merged): **§45 end-of-phase reassessment + ADR 0009** (docs-only — no code/gate/Codex). `phase-3-reassessment.md`: delivered-vs-plan, lessons harvest ("a guarantee is only real where the write path exercises it" — ≥5 instances + durable defenses), tech-debt ledger (paid #5/#7; NEW TD-P3-1..4), perf/security/usability/competitive, recommended ADRs + 2 owner decisions. ADR 0009 (`adr/0009-phase3-locations-transfers-bonds.md`): unified location tree, two-step intra-company value-conserving transfers, bonded receive (AVCO-only/F5/positive-cost), bond release = transfer + value-only duty (RBAC-immediate, F1/F2/F3), #5/#7 durable defenses. ADR README + roadmap updated. **Phase 3 implementation COMPLETE on the branch (commits 0–7); master frozen, nothing merged.**

- **2026-06-23** — **Phase 3 COMMIT 6** (branch `phase-3-overnight`; overnight run, NOT merged): **RBAC per-role split + Phase-3 demo seed + API contracts.** `entitlements.ts` adds `warehouse` (stock movement, no bond/POS) + `bond_officer` (holds BOTH bond perms for RBAC-immediate clearance, no POS/catalog) roles — operational separation of duties; `tenant_admin` still holds all. `seedPhase3` (self-contained, run-once on a fresh tenant) builds through the SERVICES: a unified location tree (warehouse→zone→2 bins with the capacity seam), store, bonded WH; 2 AVCO products/SKUs; one COMPLETED + one IN-FLIGHT transfer; a bonded receipt (40@1500) + bond release (25, duty 1875+tax 625). New `phase-3-api-contracts.md`. **No schema/migration change → HARD GATE: costing untouched.** Tests: `phase3.rls.test.ts` (DB-gated, runs the seed end-to-end + asserts tree/transfer-status/bond math) + 2 entitlements unit tests. Gates: check-types 0, biome clean, mojibake clean; fresh PG18 0000→0016 + **db 65→70, api 21/21, zero skips**. **Codex review: 0 findings (all 5 categories clean).** Detail in `OVERNIGHT-LOG.md`.

- **2026-06-23** — **Phase 3 COMMIT 5** (branch `phase-3-overnight`, off `phase-3-commit-3` HEAD `fe11d67`; overnight autonomous run, NOT merged): **bond release + duty (INV-4/5).** A release = an approved bonded→released `stock_transfer` (reuses commit-2/3 create→ship→receive; value conserved per INV-2) + a value-only duty/tax `valuation_adjustment` (qtyDelta=0, valueDeltaMinor=duty+tax) via the EXISTING AVCO value-only seam — an INTENTIONAL value-ADD (raises released cost basis), not conservation. New tenant-owned `bond_release` + `bond_release_line` (migration `0016_bond_release.sql`, ENABLE+FORCE fail-closed RLS hand-appended; coverage gate green). F4-class: `company_id` denormalized + 3-col composite FKs `(tenant,company,location)` on source+dest. F5: each line enforced against the `bond_receipt_line.costing_method_applied` stamp (AVCO-only). RBAC-immediate: ONE `bond.release` mutation requiring BOTH `bond.release`+`bond.approve_release`, emits only `inventory.bond_released` (full per-line contract); `requested/approved` events stay RESERVED (NOT in `DomainEventType`). **`costing.ts` UNTOUCHED by this commit (HARD GATE held; `costing.rls.test.ts` byte-identical).** **Codex review: 0 CRITICAL, 2 HIGH + 1 MEDIUM, all FIXED:** F1 — F5 stamp was only a GATE while `applyValuation` re-resolved costing LIVE (tenant flip avco→fifo, not blocked by #7, → throw/silent-zero-value); fixed by re-resolving LIVE via the same `resolveCostingMethod` and rejecting drift. F2 — TOCTOU over-release (lock-free on-hand read before the transfer issue); fixed with the same per-cell advisory lock `appendStockMovement` uses, before the read. F3 — order-dependent per-line value attribution for duplicate-SKU lines; replaced with a deterministic per-SKU proportional-by-qty split. Tests: `bond_release.rls.test.ts` (10, incl. F1/F2/F3 regressions) + `bond.release` ROUTER write-path proof in `vs1.integration.test.ts` (avg_cost cells actually move bonded−6000/store+6400, event contract, audit). Gates: check-types 0, biome clean, mojibake clean; fresh PG18 migrate **0000→0016** + **db 65/65 + api 21/21, zero skips**. Detail in `OVERNIGHT-LOG.md`. **(prior, MERGED: commit 4 `897f5fe`+`c9e552f` bonded receiving + INV-3.)**

- **2026-06-22** — **Phase 3 COMMIT 3** (branch `phase-3-commit-3`, off master `dbe77e2`; STOPPED for owner review, NOT merged): transfer **VALUE conservation**. `costing.ts` **+207/−0** purely additive — new `applyTransferInValuation`: AVCO reuses the frozen receipt(`floor(V/q)`)+value-only(remainder) seams (net `+V`, zero AVCO change); FIFO is a new additive value-EXACT integer layer set `(q−r)@floor(V/q)`+`r@ceil(V/q)` (Codex-F1). **HARD GATE PASSED:** `costing.rls.test.ts` byte-identical (empty diff) — frozen costing suite passes UNEDITED. **Schema collision found+resolved with ZERO schema change:** the 2-layer F1 set collides with `valuation_layer UNIQUE(tenant_id, source_movement_id)` (FIFO-receipt replay guard) → anchored the two layers to the leg's two real distinct movements (base→transfer_in, remainder→paired transfer_out); no constraint relaxation. `transfer.ts` `moveLine` valuates both legs for SKU lines (product-level = qty only); ship/receive/cancel return `{transfer, lineValues}`; cancel-after-ship returns value to source (no transit-node orphan). Router events (`transfer_dispatched`/`received`/`cancelled`) now carry per-line value + top-level aggregate. Tests: `transfer.value.rls.test.ts` (AVCO + FIFO conservation + F1 penny `V=201,q=2`→201 + F2 no-blend) + INV-2 router write-path gate. **db 45→48, api 16→17.** **Also fixed a repo-wide CI gap:** turbo strict env-mode was stripping `RLS_TEST_DATABASE_URL` → the `db-rls` job had been SKIPPING every DB-gated suite (last master log: db 22 passed/23 skipped, api 13 skipped); added `passThroughEnv` to the `test` task → turbo now runs db 48/48 + api 17/17 (zero skips). Gates: check 163/0, check-types 6/6, mojibake clean; real-Postgres migrate 0000–0014 + db 48/48 + api 17/17, costing suite UNEDITED.

- **2026-06-22** — **Phase 3 COMMIT 2** (branch `phase-3-commit-2`, off master `767fd53`): transfer model + **quantity conservation** (migration `0014_cheerful_victor_mancha.sql`). New tenant-owned `stock_transfer` + `stock_transfer_line` (ENABLE+FORCE RLS, coverage gate green). Two-step intra-company transfer: ship (source → per-transfer in-transit node) + receive (node → dest) via the sole ledger mutator (new `transfer_out`/`transfer_in` movement types) → qty conserved at every step; status flow draft→shipped→received/cancelled; intra-company-only via composite FKs on all three endpoints `(tenant_id, company_id, location_id) → location` + a service guard; date seams; events created/dispatched/received/cancelled (value fields reserved null until commit 3). Quantity-only by design — `applyValuation` NOT called, so `avg_cost`/`valuation_layer` lag until commit 3 (documented, not the #8 bug). New entitlements `inventory.transfer`/`inventory.transfer_receive`. Tests: qty conservation through the in-transit node; inter-company + cross-tenant rejection (router + raw DB composite FK); no-double-receive; no-cancel-after-received. Gates: check 162/0, check-types 6/6, test 7/7, mojibake clean; DB-gated migrate 0000–0014 + db **45/45** (regression) + api **16/16** (+2). STOPPED for owner review before commit 3.

- **2026-06-22** — **Phase 3 COMMIT 1** (branch `phase-3-commit-1`, off master `226d255`): unified self-referential location model (migration `0013_wide_bill_hollister.sql`, expand-only). `location.type` → `text({enum})` + DB CHECK (11 types incl. the warehouse hierarchy + `in_transit`); `parent_location_id` self-FK as a **composite FK** `(tenant_id, company_id, parent_location_id) → location(tenant_id, company_id, id)` so a child shares tenant+company with its parent at the DB layer (a CHECK can't read the parent — Codex-F3 lesson; reuses the commit-0 `(tenant_id,company_id,id)` UNIQUE target); four flags (`is_sellable`/`is_quarantine`/`is_bonded`/`is_transit`); nullable `max_weight`/`max_volume` bin-capacity seam (no routing). Per-invariant write-path gate: raw cross-company + cross-tenant child inserts are DB-rejected (proven). Caught a type error: narrowing `type` to the enum broke a seed call-site passing `string` → fixed by narrowing the param chain (not a cast). Gates: check 159/0, check-types 6/6, test 7/7, mojibake clean; DB-gated migrate 0000–0013 + db **45/45** (regression unchanged) + api **14/14** (+1). Coverage gate green. STOPPED for owner review before commit 2.

- **2026-06-22** — **Phase 3 COMMIT 0** (branch `phase-3-commit-0`, off master post-PR#11): parked debt #5 + #7, expand-only (migration `0012_mute_marvel_apes.sql`). #5: `UNIQUE(tenant_id,id)` on company/location/product/sku/lot + `UNIQUE(tenant_id,company_id,id)` on location (Codex F3) + an additive composite FK `location(tenant_id,company_id)→company(tenant_id,id)` (existing plain FK kept) — cross-tenant `location→company` now **DB-layer-impossible** (proven by a raw-insert test that bypasses the router guard). #7: set-once costing-method DB-trigger on product/sku with **F4** nullable-`sku_id` coverage (product-level movement locks the SKU) — proven by a raw-UPDATE-after-movement test. **Hand-fixed a real bug:** drizzle-kit emitted the composite FK before its target UNIQUE (would fail) → reordered. Gates: check 158/0, check-types 6/6, test 7/7, mojibake clean; DB-gated migrate 0000–0012 + db **45/45** (regression unchanged) + api **13/13** (+2 commit-0 tests); coverage gate green. STOPPED for owner review before commit 1.

- **2026-06-22** — **Phase 3 plan: Codex plan-review gate cleared** (PR #11, `4c8a550`). Adversarial PLAN review before any code found **2 CRITICAL + 3 HIGH**, all valid, all resolved in plan/module-spec: F1 (FIFO single aggregate layer lossy → value-EXACT integer layer set), F2 (shared in-transit blends value across concurrent transfers → PER-TRANSFER in-transit node), F3 (intra-company can't be a CHECK → denormalized `company_id` + `UNIQUE(tenant_id,company_id,id)` + composite FKs; folds into commit 0), F4 (set-once trigger missed product-level movements for a SKU; folds into commit 0), F5 (bonded-AVCO not durable → stamp resolved method on `bond_receipt_line`). **F1/F2 refine owner-locked §I.2/§I.3** — surfaced for owner sign-off before commit 0 (they affect commit 3, not commit 0). Owner approved all 6 defaults earlier. **Commit 0 HELD pending owner blessing of the F1/F2 refinements.** Separately: **design-language skill + UI reference baseline** = docs-only **PR #12** (`docs-design-language`; NOT Phase 3; governs UI from Phase 4).

- **2026-06-22** — **Phase 3 planning packet REVISED** (branch `phase-3-planning`, docs-only) per refined owner directives: parked debt (#5 composite-FK + #7 set-once DB-trigger) paid **first**, before new tables; **ONE unified self-referential `location` tree** (`parent_location_id`) replacing separate zone/aisle/rack/bin tables, + behaviour flags (`is_sellable/is_quarantine/is_bonded/is_transit`) + bin capacity seam; **two-step intra-company-only** transfers (in-transit node, date seams; inter-company blocked); **bond-release duty reuses the existing value-only `valuation_adjustment` seam** (no new costing); **generic** customs/landed-cost reference seams. Transfer value-conservation refined: AVCO conserves exactly by reusing receipt + value-only seams (no frozen change); only FIFO transfer needs an additive primitive. plan/gap/module-spec/event-map updated; PR #11.

- **2026-06-22** — **Phase 3 planning packet** (branch `phase-3-planning`, docs-only): drafted `phase-3-implementation-plan.md`, `phase-3-gap-analysis.md`, `module-specs/locations-warehouses-bonds.md`, `event-map-phase3.md`, `competitive/locations-warehouses-bonds.md`. Verified the existing spine against master `eabf98e` (location.type is bare unconstrained text; NO composite/`unique(tenant_id,id)` FKs anywhere — H1 lives at the DB layer; costing is SKU×location and method resolves product→category→tenant NOT location ⇒ transfers are never mixed-method). **#5 composite-FK pulled INTO Phase-3 scope** (new tables born composite; referenced tables get `unique(tenant_id,id)`). Designed transfer **value conservation** (move exact integer `V`, no rounding ⇒ #6 not a blocker; additive touch to frozen `costing.ts` flagged for owner). Per-invariant ownership + the recognized-class write-path-invokes-service gate baked into the plan (INV-1..INV-6). Proposed ONE implementation pass (structural CRUD over a proven spine) with a single risk-justified checkpoint after the transfer-value commit. AGENTS.md/CLAUDE.md/roadmap synced. **No code; STOP for owner approval of 6 open decisions.**

- **2026-06-22** — **Phase 2 MERGED to master** (PR #9, merge commit `72b2100`): behavior pass (item 1 single resolver + item 3 M1 event-contract normalization + reserved nullable fields) landed on master; CI 4/4 green (Quality gates, E2E, Docker build, RLS real-Postgres); local == origin == GitHub master == `72b2100`. **Housekeeping:** archived the Phase-2 commit-by-commit narrative into `phase-2-complete.md` (moved out of PROGRESS); reset the RUN STATUS current-step to a lean block; re-stamped `test-matrix.md` last-verified to `72b2100`; confirmed the parked-items list in roadmap + PROGRESS. **Phase 2 done. Phase 3 kickoff awaits a separate owner GO.**

- **2026-06-22** — Phase 2 **reserved-field fix** (branch `phase-2-behavior`): reserve two deferred event fields as **present-but-null** instead of absent, so binding them in Phase 5 is additive not breaking. `inventory.adjusted` → `approvedBy: null` (audit-critical; §22 approval workflow); `inventory.valuation_updated` → `totalValueMinor: null` + `qtyOnHandBase: null` (qty=0⟺value=0 integrity fields; bound when `ValuationResult` is extended). Additive nullable only — no behavior/schema change. Map contracts updated; integration test asserts the keys are PRESENT (`toHaveProperty`) so the reservation can't regress. Gates: check 157/0 · check-types 6/6 · test 7/7 · DB-gated db **45/45**, api **11/11**.

- **2026-06-22** — Phase 2 **behavior item 3** (branch `phase-2-behavior`): **M1 event-contract normalization** — emitted inventory events now match `event-map-phase2.md` ("payload shapes locked" is now TRUE). `emitEvent` injects a server-set `occurredAt` into **every** payload (applied last → producer can't override; §14). Field alignment: `received` carries `productId` + reserved `serialIds:null`; `adjusted` keeps `cogsMinor` (value moved through valuation; map corrected); `count_posted` aligned to the base/Minor line contract + top-level `locationId`/`currency`/`scale` (added to `postStockCount` return; service `adjustments` names preserved, only the event normalized). `valuation_updated`/`stock_discrepancy` maps aligned to as-built (totalValue/qtyOnHand enrichment → Phase 5; stock_discrepancy stays product-level until #8/Phase 4). `lot_expiring`/`lot_expired` marked DEFERRED, `uom_converted` FOLDED (kept as locked contracts, not in `DomainEventType`). `revalued` + `stock_discrepancy_reviewed` added to the catalog + consumer matrix. Gates: check 157/0 · check-types 6/6 · test 7/7 · DB-gated db **45/45** (+1 occurredAt test) · api **11/11**. Only test edit = additive occurredAt contract guard. **Item 2 (`cost_reconciliation` emit) stays Phase-4-deferred (#8, owner-ratified 2026-06-22).**

- **2026-06-22** — Phase 2 **Commit 3** (branch `phase-2-implementation`): costing storage group added — `avg_cost` (AVCO source-of-truth projection with `total_value_minor` + `qty_on_hand`) and `valuation_layer` (FIFO layer storage with `received_at`, `seq`, `qty_remaining`, `unit_cost_minor`), plus `organization.costing_method` and `organization.barcode_parser_config`. Migration `0007_overconfident_junta.sql` includes FK/index/unique/check constraints and fail-closed RLS for `avg_cost` + `valuation_layer`; DB-level invariant added: `qty_on_hand <> 0 OR total_value_minor = 0` to mechanically block orphaned AVCO value at zero stock. Verified: static RLS coverage gate 5/5, full migration chain applied in a throwaway Postgres 18 container, `check:mojibake`, `check`, `check-types`, `test` green. **Next: rich seed foundation / costing resolver services.**

- **2026-06-22** — Phase 2 **Commit 2** (branch `phase-2-implementation`): tracking schema group added — `lot` (SKU batch/expiry/status) and `serial` (serial stub with optional lot link), plus nullable `stock_ledger.lot_id`, `serial_id`, and unit-cost money triplet (`unit_cost_minor`, `cost_currency`, `cost_scale`). Widened `stock_ledger.qty_delta` and `balance_after` from int4 to int8 for base-unit quantities (eaches/grams/etc.). Migration `0006_many_synch.sql` includes FK/index/unique/check constraints and fail-closed RLS for `lot` + `serial`; `StockLedger.append` accepts optional lot/serial/cost fields and preserves policy-neutral behavior. Verified: static RLS coverage gate 5/5, full migration chain applied in a throwaway Postgres 18 container, `check`, `check-types`, `test` green. **Next: Commit 3 costing storage (`avg_cost`, `valuation_layer`, config columns).**

- **2026-06-22** — Phase 2 **Commit 1** (branch `phase-2-implementation`): catalog schema group added in approved order — `category`, `brand`, `variant`, `sku`, `barcode`, `unit_of_measure`, `uom_conversion`, plus nullable `product` extensions (`category_id`, `brand_id`, `base_uom_id`, `costing_method`, `tracking_mode`). Migration `0005_adorable_harrier.sql` includes FK/index/unique/check constraints and fail-closed RLS (`ENABLE` + `FORCE` + `tenant_isolation`) for every new tenant-owned table. Audit fix: `uom_conversion` scoped uniqueness uses `UNIQUE NULLS NOT DISTINCT` so tenant/category/product/SKU-level conversion rows cannot duplicate through nullable scope columns. Verified: static RLS coverage gate 5/5, full migration chain applied in a throwaway Postgres 18 container, `check:mojibake`, `check`, `check-types`, `test` green. **Next: Commit 2 tracking schema (lot/serial + stock ledger nullable seams).**

- **2026-06-22** — Phase 2 **Commit 0** (branch `phase-2-implementation`): docs-only planning artifacts added (`event-map-phase2.md`, `inventory-screen-map.md`), approved implementation plan updated with the no-native-`pgEnum` schema convention, root `CLAUDE.md` points future agents at the Phase-2 docs, and `tenant-isolation-coverage.test.ts` added as the mechanical RLS coverage gate. Gate statically enumerates Drizzle schema tables and requires every tenant-owned table to have **ENABLE + FORCE + `tenant_isolation` policy** coverage (or an explicit exclusion); red→green demonstrated with a temporary uncovered table and removed before commit. Gates green: `check`, `check-types`, `test`, `check:mojibake`. **Stop here for review; no Phase-2 schema/resolver/services/routers/UI.**

- **2026-06-22** — VS#1 **Commit 7** (PR #1) — **VS#1 COMPLETE**: §32 end-to-end integration test through the oRPC routers (Org→Company→Location→Product→Receipt→Sale→Invoice→Report) vs real Postgres — proves POS idempotency (same key ⇒ one sale, on-hand 7), permission denial (cashier can't create products), report totals match (USD 3000, count 1). CI `db-rls` job runs bootstrap→migrate→`bun run test` with DATABASE_URL+RLS_TEST_DATABASE_URL as `retailos_app` (+ dev-only auth env) so RLS + service + e2e tests all execute under the non-privileged role. Lesson: env-core blocks server vars under happy-dom → `// @vitest-environment node` + lazy imports in skip-guarded beforeAll. Codex: 0 findings. Gates green.

- **2026-06-22** — VS#1 **Commit 6** (PR #1): oRPC routers for the §32 flow — tenant.setActive, company/location/product.create, inventory.receive (→receipt ledger + inventory.received event), pos.createSale (idempotent; sale+lines+ledger deductions+invoice+audit+sale.created event; advisory-locked gapless numbers), reports.salesBasic (grouped by currency). Minimal RBAC (`entitlements.ts`: tenant_admin/manager/cashier), enforced per-route inside the tenant tx. Codex review: 4 HIGH → 3 fixed (cross-tenant FK refs validated via RLS-scoped reads since FK checks bypass RLS; non-negative price; multi-currency report), 1 deferred (oversell = §14 business decision, logged). Gates green; routers type-checked + 4 RBAC tests. (Full §32 e2e through routers = Commit 7.)

- **2026-06-22** — VS#1 **Commit 5** (PR #1): transactional outbox. `emitEvent(tx, ctx, {type, version?, payload})` writes one `outbox_event` row in the SAME tx (versioned envelope = the row: type/version/tenant/correlation/request/created_at + jsonb payload). `DomainEventType` consts (inventory.received, sale.created). No dispatcher/consumers/Svix/DLQ yet (deferred). Tests incl. same-tx rollback atomicity (rolled-back tx emits no event). Codex: 0 findings. 18 db tests vs real PG; gates green.

- **2026-06-22** — VS#1 **Commit 4** (PR #1): standardized request context + tenant guard. `RequestContext` `{tenantId, organizationId, actorUserId, employeeId?, sessionId?, impersonatorUserId?, requestId, correlationId, source, deploymentMode}` (superset of db ServiceContext) built fail-closed by `buildRequestContext` (UNAUTHORIZED w/o user, FORBIDDEN w/o active org); `tenantProcedure` = protectedProcedure + tenant guard. `tenantId` comes ONLY from session.activeOrganizationId (not client headers). Added `DEPLOYMENT_MODE` env; `check-types` added to api+env (now 6 pkgs gated). Codex review: 0 findings. Gates green; 3 guard unit tests.

- **2026-06-22** — VS#1 **Commit 3** (PR #1): core services in `packages/db/src/services/` — Money (integer minor units, no rounding yet), StockLedger (sole stock mutator; advisory-lock serialized `balance_after`), Idempotency (canonical payload-hash, advisory-lock pre-select), Audit. New `idempotency_key` table + RLS (now 12 tenant tables). Money columns widened int4→bigint(mode:number). Codex review: 3 HIGH fixed (idempotency race, non-canonical hash, money safe-int/int4) + regression tests. 16 db tests pass vs real PG; gates green. (Note: PROGRESS.md UTF-8 was corrupted by a `perl -0pi` run and restored — never run perl in-place on these docs.)

- **2026-06-22** — VS#1 **Commit 2** (`e9b711e`, PR #1): migrations + fail-closed RLS + 3-role bootstrap (ADR 0006). `roles.sql` (idempotent, superuser, pre-migration): owner/migrator/app all NOSUPERUSER/NOBYPASSRLS/NOCREATEDB/NOCREATEROLE; migrator SET role→owner; revokes PUBLIC schema CREATE + any owner-membership from app. Migration 0001 ENABLE+FORCE RLS + fail-closed policy on 11 tenant tables. URL split (app runtime / migrator migrations). withTenant rejects empty tenant. CI `db-rls` job (real Postgres: bootstrap→migrate→test). 5 RLS tests pass vs real PG. Codex review: 2 HIGH role-hardening fixed, 0 CRIT. Gates green.

- **2026-06-21** — VS#1 **Commit 1** (`c8e7ab1`): schema + Better Auth org/admin + tenant-scoped seed scaffold. Domain tables (company/location/membership/product/stock_ledger/sale/sale_line/invoice/audit_log/outbox_event/number_block) with tenant cols from day one; `withTenant()` GUC primitive; `seeds/` via injected Better-Auth provisioner (no bypass); `tenant_id` text (BA nanoid), domain PKs uuid; added `check-types` to db+auth. NO migration/RLS yet. Gates green (check 115, check-types 4, test 6). **Stopped for review before Commit 2 (Migration + RLS).** Approved 8-step sequence recorded in `vertical-slice-1.md`.

- **2026-06-21** — Turbo test orchestration (`7ea29b2`): per-package `test` scripts + re-exported `vitest.config.ts` (6 pkgs); root `test`→`turbo run test`; dead `lint` task removed (Biome single-root). Resolves audit M6.

- **2026-06-21** — Phase-0 audit-only red-team pass (5 parallel read-only agents): **0 CRITICAL / 0 HIGH**. Report `phase-0-audit.md`. Resolved 5 stale-metadata §40 contradictions (ADR index +0004/0005; roadmap Phase-0→Complete; checklist Docker row + counts; quality-security-ops gate table; CLAUDE.md +docker-and-cicd/tech-stack refs). Dismissed M7 (postgres-18 volume) via live `docker image inspect` — PG18 uses `PGDATA=/var/lib/postgresql/18/docker` so our parent mount is correct; new lessons-learned entry. Queued: turbo test/lint no-ops, deferred §43 gates, §44 perf gate.

- **2026-06-21** — Deps: better-auth+expo 1.6.11→1.6.20, biome deduped to 2.5.0, lucide-react unified to catalog ^1.21.0; all gates green post-bump. Docker: web image multi-stage distroless-bun 2.38GB→168MB; CI +docker job (GHA cache + ≤350MB size gate, checkout@v5); prod compose +resource-limits/log-rotation; consolidated docs into docker-and-cicd.md. ADR-0004 (central-infra reuse) + ADR-0005 (Product Intelligence Layer deferred).

- **2026-06-21** — Deps: lucide-react aligned to catalog ^1.21.0 (deduped), Better Auth + @better-auth/expo 1.6.11→1.6.20, Biome root 2.4.16→2.5.0 (deduped). ADRs 0004 (central-infra reuse) + 0005 (Product Intelligence Layer deferred). docs/architecture/docker-and-cicd.md added; CI gains docker build+GHA cache+size gate; prod compose resource limits+log rotation. All gates green.

- **2026-06-21** — Docker: optimized apps/web image 2.38GB→168MB (~93%) via multi-stage (turbo prune --docker → builder → distroless-bun runtime shipping only Nitro .output), non-root; verified boots+listens. Hardened .dockerignore. Patterns adapted from same-stack heimdallone reference.

- **2026-06-21** — CI fix: fumadocs (docs-site addon) excluded from build gate — shiki onig.wasm can't bundle under rolldown/Vite8; root build now 'turbo build --filter=!fumadocs'; engine:'js' kept for dev; turbo build outputs +.output/.nitro. All gates green locally.

- **2026-06-21** — PHASE 0 COMPLETE: phase-0-checklist.md added (§46 scoreboard); all governance + architecture-review docs present; gates green (check/check-types/test); merging branch → master. Remaining = CI first-run green + docker image build verify (run on push) + Phase-1 features.

- **2026-06-21** — Phase-0 D complete + GREEN: check ✅ / check-types ✅ / test ✅ (3 tests). Fixed biome nested-root (fumadocs root:false), overrode framework-incompatible rules, excluded vendored skills + docs-site, fixed 5 real lint errors. Pinned postgres:18-alpine. tech-stack.md added (versions/compat verified; flagged lucide-react split + biome 2.4.16/2.5.0 dedup as follow-ups).

- **2026-06-21** — D config wired: vitest(happy-dom)+smoke test → `bun run test` PASSES (3 tests); playwright.config+e2e placeholder; .github/workflows/ci.yml; root test/lint/e2e scripts + lint-staged; turbo test task; docker redis+minio (base compose); fixed broken `bun test` pre-commit → lint-staged + check-types.

- **2026-06-21** — Central infra (KareTech VPS, save resources): provisioned `retailos` DB (least-priv role) in postgres-central; creds stored in Infisical /credentials/retailos (prod); REDIS_URL→redis-shared/3. Added docker-compose.prod.yml (VPS override reuses central via pangolin; others use self-contained base compose). Hybrid confirmed.

- **2026-06-21** — Automation lane: added SessionStart hook + scripts/log-progress.sh + CLAUDE.md progress protocol (lean+linked). Multi-agent Work-lanes table added to PROGRESS.md.

- **2026-06-21** — Phase 0 started. Plan approved (docs + foundation config, no feature code; competitive matrices deferred per-module). Branch `phase-0-architecture-foundation` created; `PROGRESS.md` added. Confirmed `base-lyra` = Base UI. Prior this session (on `master`): UI inventory v2 complete, registry config corrected & live-verified, `lessons-learned.md` seeded (12 entries).
