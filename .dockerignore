**/node_modules
.git
.github

# Agent / tooling state — never needed in an image build context
.claude
.agents
**/.cache

# Docs & design assets (not needed to build/run the app)
docs

# Test artifacts & coverage (dev-only)
**/coverage
**/test-results
**/playwright-report
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
**/*.spec.tsx
e2e

**/dist
**/build
**/.next
**/.nuxt
**/.output
**/.svelte-kit
**/.astro
**/.turbo
.turbo

**/.wrangler
**/.alchemy
**/.expo
**/.vercel
*.log

Dockerfile
**/Dockerfile
docker-compose.yml

# Secrets stay out of image layers; runtime env comes from compose env_file,
# build-time public values come from compose build args
**/.env
**/.env.*
!**/.env.example
